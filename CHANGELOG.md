# Changelog

## [Unreleased]

Findings from an independent pre-publish review, run as a fresh reviewer against
ADR-022's six irreversibility lines. Two real defects on the lines themselves,
both fixed here; the rest are first-run and disclosure gaps. Recorded with the
six Keep-a-Changelog types (the workspace convention as of 2026-08-01) rather
than this file's older `[feat]`/`[fix]` tags.

### Added

- **The sync coordinator — `enableSync(store, opts)` — Stage 3 goes from a frozen contract to a
  working local-first sync loop.** `src/coordinator.ts` implements every numbered behavior in
  ADR-006 rev d's "Coordinator semantics": the outbox tap (`store.subscribe()` filtered on
  `origin === "local-mutation"`, an accept-list), push/pull with confirmation gated solely by the
  pull channel's `confirmedMutations` (never a push ack alone), version-aware apply with the
  four-valued comparator, echo suppression, the ADR-004 boundary, `local: true` exclusion,
  exponential-backoff-with-jitter retry (D9), independent per-subscription reset jitter (D10),
  schema-mismatch suspension (D12), and same-device sibling-outbox recovery (§1c/D4). `src/
  coordinator-conformance.ts` + `src/coordinator-conformance.spec.ts` add 35 tests proving all 14
  named obligations in `SYNC_CONTRACT_COVERAGE.coordinator` plus the ADR's remaining numbered
  clauses — the highest-risk ones **watched to fail**: the exact mechanism was temporarily broken
  in `coordinator.ts`, the corresponding test confirmed red, then reverted (recorded on DAN-776,
  not encoded as permanent mutants — there is exactly one `enableSync()` to mutate against, unlike
  the many swappable adapters `sync-conformance.ts` protects).

  **Not exported from `index.ts`** — same ADR-022 lines 1-2 precedent as `sync-types.ts` /
  `sync-conformance.ts`. Suite goes 522 → 557 (plus `packages/mcp`'s 29, unchanged).

  **Two implementation gaps found writing this, both resolved in ADR-006 itself** ("Implementation
  note, part 2"): §1's "`reject` triggers the existing rollback machinery" describes a mechanism
  `transactions.ts` cannot offer post-commit (`rollback()` is only reachable before `commit()`
  splices the transaction out) — the fix reads `EntityEvent.previousData`, already captured for
  free, and reverts under a new `origin: "undo"` write channel instead. And `transform`'s "rekey
  entity... one move event" has no single-event primitive to build from (`EntityStore` has no
  rename), so it composes as `set(newId)` + `remove(oldId)`.

  **A third gap — this one an actual bug, not a spec gap — found by an independent fresh-context
  review of the diff before landing:** the transform branch applied the server's corrected entity
  immediately (per D2) but never replayed a second, still-unconfirmed writer to the same key on
  top of it — the exact hazard the reject-path fix above was written to prevent, just left unfixed
  on the sibling branch. Also fixed from the same review: a thrown `pull()` was an unhandled
  promise rejection that permanently killed that subscription (now retries with the same
  backoff+jitter shape as push); a rejected *adopted* (sibling-recovered) outbox entry always
  reverted to non-existence regardless of what it actually overwrote, because
  `StrandedOutboxEntry` never carried `previousData`; and a schema-suspended outbox had no way to
  resume (`resumeAfterSchemaMigration()` added — D12 says a mismatch suspends the outbox but never
  says how suspension lifts, and the coordinator cannot detect an app-level migration on its own).

- **Pagination is tested. It shipped in `0.1.0` untested, and nobody could tell.**
  `cursorPagination`, `offsetPagination` and `relayPagination` have been exported
  from `index.ts` since extraction — with **no test anywhere in this repository.**
  The only coverage lived in the Vue plugin's frozen fork of the engine, so the
  module was published untested while *looking* tested, because someone else's
  copy had tests. Chip 3 deletes that fork, so the coverage was about to go from
  misplaced to gone.

  24 unit tests ported (`src/pagination.spec.ts`), and **watched to fail** before
  being trusted: reversing the merge order turns 3 red, removing the dedup pass
  turns 1 red. Suite 497 → 521.

- **Five pagination types that existed but could not be imported.** `index.ts`
  exported `RelayPageInfo` alone, while `CursorPaginationOptions`,
  `OffsetPaginationOptions`, `RelayPaginationOptions`, `RelayEdge` and
  `RelayConnection` sat unexported in `pagination.ts`. Since `package.json`
  exposes only `"."`, there was no deep-import escape — **a caller of
  `cursorPagination` could not name the type of the options object they were
  required to pass.** Now exported; `etc/colada-db.api.md` regenerated in the same
  commit per ADR-022 line 2, after watching the gate fail first.

- **The sync wire protocol `v1`, and it is versioned in the path.**
  `docs/protocol/sync-wire-protocol-v1.md` is the first normative statement of
  what a colada-db sync *server* does — verbs, JSON shapes, status codes, where
  the cursor travels, retention obligations. `src/wire-protocol.ts` carries the
  parts that must never be re-derived by hand. ADR-023 records the decision.

  **The gap was measurable, not theoretical:** two people implementing
  `restAdapter` would have built mutually incompatible servers and both would
  have passed the client conformance kit, which checks that an adapter honours
  the contract and cannot check that two adapters agree with each other.

  **The framing "should we ship a reference server?" was wrong**, and correcting
  it is the substance of ADR-023. It complects two decisions with opposite
  reversibility: the wire protocol can never be changed once a backend speaks it
  (ADR-022 line 5), while a reference server is a package that can be rewritten
  or thrown away. Pricing them together guarantees getting one wrong. And the
  protocol was never optional — the first adapter to ship would have defined it
  by accident, which is the calcification ADR-006 was frozen early to prevent,
  recurring one level down where nobody was watching.

  **The version is a path segment (`/sync/v1/pull`), and that is what makes an
  irreversible artifact survivable.** A published version freezes forever;
  breaking changes ship as a new path served alongside the old one. Being wrong
  about `v1` costs a `v2` rather than a migration on somebody else's backend. A
  test pins the path so a future edit has to be a deliberate, visible act.

  **HTTP status codes carry transport outcomes; the body carries verdicts.**
  This is the highest-value clause, because it mechanizes the most dangerous
  confusion in ADR-006 — a transient failure reported as a permanent `reject`
  destroys a valid user write silently and forever. `classifyWireStatus()` makes
  that call once instead of asking every adapter author to remember a table:
  `408`/`429` are transient despite being 4xx, `409` routes to a schema channel
  that *suspends* the outbox rather than draining it, and **an unrecognized
  status defaults to transient, never permanent** — a retried write is
  recoverable and a wrongly-discarded one is not.

  **Authentication is deliberately absent.** No identity model, no token format,
  no session. Credentials ride standard HTTP headers the protocol never
  inspects. A protocol that names an identity model inherits its lock-in, and
  per ADR-0017 an identity model is what an *organization* needs operated — so
  baking one into the free protocol puts the open-core split in the wrong place.

  **Retention is resolved by a split:** retention of the change *feed* is not
  retention of the *data*. Entities live forever; the log lives for a bounded
  window; a client outside the window takes a `reset`, which is a first-class
  path and not an error. The optional `retentionSeconds` field lets a client
  resync deliberately instead of discovering expiry through a failed pull.

  17 new tests pin the normative claims, including the numeric-string trap
  (`"10"` must order after `"9"`, or a backend silently reverses its own
  history). Suite is 480 → 497.

- **The SyncAdapter conformance kit — the spec stops being a document.**
  `src/sync-conformance.ts` exports `runSyncAdapterContract()`, one contract
  suite any third-party adapter author can run against their own backend, plus
  `src/sync-types.ts` carrying ADR-006 rev d's frozen wire contract as
  TypeScript for the first time. 37 new tests; the suite goes from 443 to 480.

  **The gap it closes was measurable.** `docs/specs/sync-adapter.allium` says in
  its own header that it was written so an executable conformance kit could be
  generated from it, and nothing had. Running `allium plan` shows why that
  mattered: the spec yields 157 test obligations and **the `@invariant` blocks on
  `contract SyncTransport` produce none of them** — only `contract_signature`
  obligations come out of a contract, so the prose inside an `@invariant` is
  documentation the tooling never turns into a check. That is the same defect
  this repo already has lessons about, one level up: a rule nothing fires, an
  invariant nothing establishes. This kit is the mechanism that makes those
  invariants binding on an adapter.

  **It had to exist before `restAdapter`, not after.** An adapter shipped first
  passes because it passes — correct behaviour gets defined by implementation,
  one level below the contract that was just frozen to prevent exactly that.
  With the kit first, the first adapter is *proven* to conform rather than
  definitionally conforming, and so is every adapter after it.

  **Watched to fail, and it caught its own defect doing so.** Pointing the kit at
  a deliberately broken adapter turned six properties red — and revealed that
  `honours limit as a ceiling` was reporting PASS against an adapter that
  over-serves `limit`, because an early `return` inside the `it` body ran when
  the optional `seedRemote` hook was absent. A bare `return` ends a Vitest test
  successfully, so the kit was reporting conformance on a question it never
  evaluated. Guards moved to describe-level `it.skip`, so an unexercised property
  is visible instead of counted as a pass.

  Coverage is declared rather than inferred from absence: `SYNC_CONTRACT_COVERAGE`
  names which obligations the kit checks, which belong to the unbuilt coordinator
  and cannot be observed from outside an adapter, and which need a real backend
  over real time. A reader finding `AtMostOnePushInFlightPerClient` in the
  coordinator list is being told it is real and somebody else's to honour, not
  that it was forgotten.

  Neither new module is re-exported from `src/index.ts` — ADR-022 lines 1 and 2,
  the same rule `engine-conformance.ts` follows. Verified rather than assumed:
  the pack manifest still reports 11 entries and the API report still matches.

- **`.coderabbit.yaml` — the automated reviewer is now configured, because it
  became a merge gate.** CodeRabbit has reviewed every PR here since #10 on
  defaults, which was harmless while CI was advisory. It is not harmless now:
  `main` requires conversation resolution, so **every** unresolved CodeRabbit
  comment blocks the merge. That makes the review profile a merge-friction
  setting rather than a taste one. Set to `chill` (a nitpick and a real defect
  cost the same to clear, which trains people to resolve without reading), with
  `request_changes_workflow: false` — that one must stay off, since a "request
  changes" verdict on a repo whose sole maintainer cannot self-approve would
  deadlock `main` outright. Generated and gate-asserted artifacts are filtered
  out (`etc/colada-db.api.md`, the expected pack manifest, the lockfile), and
  `path_instructions` teach it this repo's actual rules: watch a gate fail
  before trusting it, never weaken a gate for green, the `process` guard shape,
  `engine.persistent` before any survival claim, ADR-022's lines, ADRs are
  append-only, and one CHANGELOG heading per type. Every key was verified
  against CodeRabbit's published schema before being written.

- **`SECURITY.md`** — there was no private vulnerability-disclosure path for a
  package that holds user data and ships an agent surface. Points at GitHub
  private advisories rather than an email address. (Private vulnerability
  reporting was enabled on the repo once it went public, so the link resolves.) Scope
  section names what counts (allowlist bypass on the MCP surface, matcher
  escapes, a veto that fails to veto) and what is a documented boundary rather
  than a defect (origin tags are attribution, not authentication; no encryption
  at rest).

- **CI proves the API differ can fail before trusting it to pass.**
  `check-api-report.mjs` shipped a `--selftest` that nothing invoked, while
  `check-pack-manifest.sh` and `cross-check-publish-surface.sh` both self-test
  inline on every run. The newest publish gate was the one odd one out.

- **The open-source furniture a stranger expects.** Every doc in this repo was
  written for maintainers and agents; a first-time visitor had a README and
  nothing else. Added `CONTRIBUTING.md` (verify commands, reading order, the
  append-only ADR rule, when to regenerate the api-report and the pack
  manifest), `CODE_OF_CONDUCT.md` (Contributor Covenant 2.1), and GitHub issue
  and PR templates. The templates are shaped around this codebase rather than
  the defaults: the bug form asks **which engine** and **whether
  `engine.persistent` was true**, because `sqliteEngine` silently falls back to
  an in-memory database when OPFS is unavailable and "my data disappeared" is
  the expected result in that state — that one answer decides whether a report
  is a bug or a misconfiguration. The feature form asks whether the proposal
  crosses an ADR-022 irreversibility line. `.editorconfig` and `.nvmrc` too.

### Changed

- **ADR-006 rev d — the sync-adapter contract now says one thing per question.**
  Writing the Allium specification of the contract against rev c exposed 20
  unresolved points, four of them outright contradictions where two sections of
  the same document specified different behaviour (DAN-736). **Rev d resolves all
  twenty**, and the contract now has no open questions. The contract is frozen and
  the coordinator is unbuilt, which makes this the cheapest moment these will ever
  have — ADR-022 line 5 means wire shapes stop being ours the day a real backend
  speaks them.

  Every change is additive except one deletion. **A second mutation-confirmation
  path is gone**: `§1b` offered confirmation via "a pulled checkpoint ≥ the ack's
  serverVersion", which had no field on `PullResult` to travel in, and which
  presumed versions were *totally* ordered — the exact property constraint C2
  exists to refuse, so a vector clock or hybrid logical clock stays
  representable. `confirmedMutations` is now the sole confirmation channel, and
  it costs a backend nothing extra because per-client seq tracking was already
  mandatory.

  The additions: `compareVersions` becomes a real, optional, **four**-valued
  adapter method, because three values cannot say *concurrent* — the same
  decision as 0.1.0's pre-publish widening of the version slot to
  `string | number`, since an HLC that can only report a total order is a slow
  integer. `pull()` and both `PullResult` variants gain an optional
  `subscription` name, which finally defines the partition unit the per-
  subscription checksums and resets had been referring to; the name is opaque
  and core never parses it, because an opaque name is the most a partition can be
  without letting a server-evaluated shape model reach the coordinator and
  foreclose end-to-end encryption permanently. `transform` splits into an
  immediate id remap and a deferred overlay drop, which were never one operation.
  Sibling-outbox recovery is defined as relay rather than authorship, with the
  commit-time obligation that makes it survive a public-key `clientId`.

  Also settled: `limit` is a hint, retry backoff has **no attempt ceiling and no
  dead-letter queue** (dropping a user's write silently is worse than staying
  visibly stuck — correct for a job queue, wrong for a local-first database),
  checksum verification is presence-driven rather than flag-driven, a
  `schemaVersion` mismatch suspends the outbox rather than draining it, a
  server-directed reset never invalidates in-flight seqs, and tombstone retention
  becomes a checkable relation to the oldest honoured cursor.

  The two naming drifts resolved in favour of the shipped code, not the document:
  the origin stamp is `sync-pull` (not `remote`) and the device-local flag is
  `local?: boolean` (not `sync: false`). These read like coin flips until you
  notice one side of each is on npm.

  **The contract now has no open questions, and the last two are why that
  matters.** A first draft of rev d left named-mutator rebase and priority-tiered
  hydration open, justified as "additive later, not one-way doors." That is true
  of the *fields* and false of the *contract*: adding a second way to express a
  write to a protocol other people's servers already speak forces every adapter
  thereafter to handle both — a permanent wart bought with a deferral, which is
  the exact calcification freezing this contract early was meant to prevent.

  `LocalChange` gains an optional `intent: { name, args }` while `data` stays
  required. Intent is strictly more powerful — a server replaying a named mutator
  against post-apply state rebases properly rather than judging a patch computed
  against stale state — but requiring it would require the server to run client
  code, and an existing REST API cannot execute a function from your bundle. A
  contract that requires shared code is not backend-neutral, and that is this
  package's whole identity. So intent is an adapter-level capability, exactly as
  `transform` is.

  Priority-tiered hydration needed **no wire field at all**: priority is a
  property of a subscription, and initial pulls are issued in priority order
  without being awaited. Ordering the starts is what puts gates on a phone screen
  before history backfills; awaiting would let one slow partition stall every
  partition behind it.

  Each resolution now carries a **falsification test** rather than a hedge — the
  thing that makes a decision complete without pretending it is certain.

- **AGENTS.md no longer describes the pre-publish world.** Three claims went
  stale the moment 0.1.0 shipped, in the first file every agent reads: CI was
  described as ADVISORY with a 403 receipt (it is now blocking, six required
  checks, `enforce_admins: true`); `"private": true` was described as the guard
  rail (it is gone, and the rule it enforced matters *more* now, not less —
  every publish from here reaches real consumers and cannot be un-shipped); and
  chip 3 was marked gated on the publish (that gate is lifted, which also makes
  ADR-020's cross-version ref-identity case reachable rather than hypothetical).
  A manual that confidently states the opposite of reality is worse than no
  manual, and this one is load-bearing for every agent session.

- **`main` is now fully protected — `enforce_admins` is true, and the admin
  exception is closed.** For a few hours after branch protection went on, the
  honest sentence was "CI is binding for contributors, advisory for the admin";
  it is now binding for everyone, with no direct pushes to `main` from anybody.
  ADR-021's title is literally accurate for the first time since it was written.
  Verified by watching it fail: a direct push as the repository owner is
  rejected with `protected branch hook declined`. Required approvals stay at 0 —
  a solo maintainer cannot approve their own PR, so requiring one would deadlock
  the repo; 0 keeps the process mandatory without inventing a reviewer who does
  not exist. ADR-021 also now records the lockout hazard this creates (a renamed
  CI job leaves a required context pending forever and blocks `main`) and the
  admin escape hatch, with the rule that using the hatch is fine and using it
  silently is not. `CONTRIBUTING.md` states the PR requirement and the
  `[skip ci]` trap for contributors.

- **Branch protection is live on `main`; ADR-021 no longer describes a gate it
  did not have.** Going public at the 0.1.0 publish unlocked branch protection,
  which is the condition ADR-021 named on 2026-07-23 when it recorded — honestly,
  and as its own central argument — that CI was advisory. Six required checks,
  not the five originally listed: `browser durability lane` was added, since
  requiring every check *except* the only one where storage is real would have
  been the precise error the ADR is about. Force pushes and branch deletion are
  blocked for everyone. `enforce_admins` is deliberately **false** so the solo
  maintainer keeps direct pushes, and the ADR states the resulting true rung:
  binding for contributors, advisory for the admin. The 2026-07-23 section is
  kept verbatim under a supersession note — an ADR that argues for stating the
  real enforcement level should keep the evidence that its level was once low.

- **`StorageEngine`'s `version` slot widened to `string | number`** (ADR-022
  line 2), matching `EntityEvent.version`. It was `number` while the event
  allowed both — even though the engine's own JSDoc names a *server timestamp*
  as a legitimate source, and the causal stamp it eventually wants would most
  likely be a hybrid logical clock. Both are strings. With the `version`
  semantic still open under DAN-736, the narrower type would have decided it by
  accident in the one direction publish makes unfixable: widening a return type
  afterwards breaks every consumer that reads it. No code anywhere performed
  arithmetic on it. `etc/colada-db.api.md` regenerated in the same commit — the
  gate caught the change and named the exact line before it was updated.

- **README quickstart now compiles.** `contact.value.name` failed `TS18048:
  'contact.value' is possibly 'undefined'` under `--strict` — the first code a
  new consumer copies, in the file npm renders as the landing page. Also added
  the two things it never showed: `EntityRegistry` module augmentation (the
  mechanism that makes reads typed rather than `Record<string, unknown>`), and
  an actual normalization example — the headline capability had no code in the
  README at all. Every snippet is now compiled against the packed tarball under
  both `bundler` and `nodenext` resolution.

- **`prepublishOnly` runs the whole workspace and the durability lane.** It ran
  `pnpm test` (root only, so `packages/mcp`'s 29 tests never ran) and never
  `pnpm test:browser`. Since ADR-021 records CI as advisory pending branch
  protection, the last automatic gate before a real `npm publish` was the one
  that skipped the only lane where storage is real.

- **`llms.txt` no longer denies the flagship feature exists.** It stated the
  four AI-first trust primitives were "designed and audited, not yet in this
  build"; they shipped 2026-07-19 and all four are exported from the package
  root. Source map and verify commands brought current, `packages/mcp` added,
  and what is genuinely absent (sync) stated plainly.

### Removed

- **`"private": true`.** The publish block is lifted — `colada-db` is now
  publishable. Nothing is on the registry until `npm publish` is run by hand.
  README's status block now describes a 0.x first release rather than an
  unpublished extraction, and carries npm/CI/license badges. This is ADR-022
  lines 3 and 6 going live.

### Fixed

- **The persisted-format stamp did not reach the common case (ADR-018, ADR-022
  line 1).** `formatVersion` is the only escape hatch the on-disk format has,
  it lives in the manifest index row, and that row was materialized **only when
  `setManifest`/`removeManifest` had been called.** So every database created
  on the default `hydration: "all"` path — exactly what the README quickstart
  teaches — was written with no version marker anywhere on disk. The read side
  was never the gap: both boot paths already parse and skip the index row, so
  only the write side was conditional. The row is now stamped on the first
  flush that persists anything; a coordinator that persists nothing still
  writes nothing. Four tests in `src/format-version.spec.ts`, watched to fail
  first (3 of 4 red with the fix reverted, while the "persists nothing"
  negative control stayed green in both directions).

- **A dotted matcher field silently matched nothing.** `M.eq("a.b", 2)` against
  `{ a: { b: 2 } }` returns `false` — fields are flat own-property names and
  there is no path traversal. Behavior is unchanged and deliberate (an entity
  may own a key containing a dot; reserving the character would make that key
  unfilterable), but the only thing asserting it was prose, and the failure is
  silent — on the agent surface a nested-intent filter reads as a confident
  "no matches". Now stated in the `matcher.ts` module docs, on `M`, and in
  `docs/design/matcher-semantics.md`, and pinned by three tests.

- **`npm publish --dry-run` failed on this repo, and the publish did not.** npm
  exports `npm_config_dry_run=true` into every lifecycle script it runs, so the
  nested `pnpm pack` inside `check-pack-manifest.sh` printed a file list and
  wrote no tarball — the gate then counted zero and failed. The rehearsal you
  run *because* the real thing is irreversible was the one command that broke.
  Sixth instance of this repo's recurring family, and the first in the safe
  direction (see `LESSONS.md`).

### Security

- **GHSA-frvp-7c67-39w9 cleared from the dependency tree** (moderate, CVSS 5.9 —
  path traversal in `@hono/node-server`'s `serve-static` on Windows via an
  encoded backslash). It reached the workspace transitively through
  `packages/mcp` → `@modelcontextprotocol/sdk` → `@hono/node-server@1.19.14`.
  **Published consumers were never exposed:** `colada-db` ships zero runtime
  dependencies (only the `@vue/reactivity` peer and the optional sqlite-wasm
  peer), `packages/mcp` is not on npm, and that package uses `InMemoryTransport`
  exclusively — it never constructs an HTTP server, so the vulnerable
  static-file path is unreachable in how the SDK is used here. Fixed rather than
  dismissed: SDK bumped to `^1.30.0`, which widened its own range to
  `^1.19.9 || ^2.0.5`, plus a pnpm override selecting the patched line — pnpm
  had otherwise kept the locked 1.19.14, which still satisfied the widened
  range. The override stays inside the range the SDK itself declares, so nothing
  unsupported is being forced. `pnpm audit`: no known vulnerabilities. Verified
  the bump did not disturb the agent surface — 29 tests plus the 8-check
  observe-run against the built server.

## [2026-08-01] — publish preflight: the last unmet ADR-022 precondition, and the oracle bug CI found on an unlucky seed

Preparing to publish. ADR-022 states the required order in one line — *DAN-724 decided → api-report snapshot exists → publish* — and only the first was done. Two blockers closed, and one open question answered in a way that removes a whole ADR from the publish path.

- [feat] **`scripts/check-api-report.mjs` + `etc/colada-db.api.md` — ADR-022 line 2.** The ADR's own Open section named the gap: *"Line 2 has no api-report snapshot yet. Until one exists, 'did the public surface change?' is answered by review rather than by a diff."* Publishing without it would freeze v0.1.0's public surface with no mechanical record of what it was, so no later version could prove whether it broke compatibility — and a version on the registry cannot be un-shipped. Comments are stripped deliberately (a JSDoc edit is not an API change, and a report that churns on prose trains the reader to approve without looking); everything else compares verbatim. Wired into `prepublishOnly` and CI. Watched to fail: 7/7 selftest, then a genuinely removed export (`memoryEngine`) caught and named at the exact line.
- [fix] **DAN-749 — the oracle conflated eviction with deletion at commit.** CI went red on an unlucky seed; the suite runs 200 *unseeded* schedules, so this was never version-specific. `TxCommitCmd` derived durable truth from memory residency, and `gc()` — the one command with no `txHolds` guard, because it takes no key — evicted the entity between the optimistic write and its commit. The model then read "not resident" and concluded "deleted." **The store was right:** `persist.ts` returns early on evict because *"eviction has no authority over the durability pipeline"* (ADR-013, after the DAN-621 resurrection bug), and optimistic writes live in `pendingTx` until commit graduates them. `Model.tx` now carries the same buffer. The schedule is pinned as an always-executed example — it fires at run 2 instead of run 182, so it was found by luck once and is never left to luck again.
- [docs] **ADR-006 is NOT on the publish path.** ADR-022 line 5 covers wire and protocol shapes, which reads as though DAN-736's unresolved ADR-006 contradictions would block publish. They do not: `SyncAdapter` is not exported from `src/index.ts` and appears zero times in the api report, so publishing freezes nothing about it. Line 5's only published exposure is the matcher AST (ADR-009) — `parseMatcher` / `serializeMatcher` / `MatcherNode` — which has its own ADR, a fail-closed parser, and a 5000-tree injectivity fuzz.
- [docs] AGENTS.md: the publish surface now has THREE gates, each with the question it answers, and records that `check-public-types.mjs` is not one of them (it asserts framework-*freedom*, ADR-019 — a check a surface can pass while having silently dropped an export).

## [2026-07-31] — DAN-652: every green check proved a mock — L4 puts the engines on real IndexedDB and real OPFS, and kills the page between the write and the read

The strongest durability claim this repo could previously make was *"the stand-in agreed with us."* `idbEngine` ran against `fake-indexeddb`, `sqliteEngine` against a `:memory:` database — so even L2's reopen property proved data survives dropping the *engine*, not the *process*. For a database whose entire pitch is *trust your local data*, that gap sat exactly under the pitch. This is the lane that closes it, and it is the only test here that is not a proxy for something else.

- [feat] **`pnpm test:browser` — Playwright over real Chromium** (`playwright.config.ts`, `tests/browser/`). Two specs, both write → **reload the page** → cold-boot a brand-new store → read: `idb-reload.spec.ts` against real IndexedDB, `opfs-sqlite-reload.spec.ts` against a real OPFS SQLite database in a real Worker. Deliberately a SEPARATE script — `pnpm test` stays the sub-2s vitest inner loop, and the config comment says why.
- [feat] **The fixture drives `enablePersistence`, not the raw `StorageEngine` port** (`tests/browser/fixture/`). Testing the port directly would have been simpler and would have missed the point: DAN-647 and the ADR-012–016 family all lived in the coordinator's write-behind path, and hid precisely because a synchronous engine resolves before the race window opens. The fixture is served from **source** by vite, so a green run reflects the working tree rather than a stale `dist`.
- [feat] **`engine.persistent === true` is asserted before any survival claim.** When OPFS is unavailable the worker silently falls back to an in-memory database and keeps answering every call successfully (`src/sqlite-worker.ts`). Without this assertion an in-memory run is indistinguishable from real durability at the point of failure. The playground already used this check informally ("A0 · OPFS is real"); the lane now makes it structural.
- [feat] **A negative control in each spec.** Both files boot a fresh browser profile and assert storage reads **empty**. A durability suite that cannot observe absence proves nothing — every survival assertion would still pass if `verify()` were quietly returning stale in-memory state.
- [test] **Verified by mutation, not by going green.** Suppressing the seed writes turned the 4 survival/fidelity tests red while both negative controls stayed green — so the assertions are known to depend on real data reaching real storage. The ticket's own DoR gate, which previously exited `1` (no spec) and `254` (no script), now exits **0**.
- [docs] **The `Date` divergence is pinned in both directions, as CONTRACT.** IDB structured-clones so a `Date` returns a `Date`; SQLite `JSON.stringify`s so it returns an ISO `string`. `src/types.ts` already stated this; the lane is the first *executed* evidence for it. Do not "fix" the sqlite side — a spec asserting a real `Date` round-trip there would be asserting a false contract.
- [chore] `@playwright/test` devDependency; `test-results/` + `playwright-report/` gitignored. Published surface unchanged — `check:publish-surface` (9 assertions) and `check:pack-manifest` (11 entries, both directions) still pass, since `files` ships only `LICENSE`, `README.md` and `dist`.
- [docs] `TESTING-STRATEGY.md`: L4 marked LANDED with its three load-bearing properties; priority order advanced to perf-regression pins.

## [2026-07-30] — DAN-653: the suite could only find bugs someone had already imagined — L3 property search + the L2 conformance kit, which found a real divergence on its first run

Every durability test in this repo was example-based: a human wrote down an interleaving they had already thought of. That finds the anticipated bug and, by construction, nothing else — and the entire ADR-012–016 family plus DAN-647 lived in orderings nobody wrote down. Two additions from `TESTING-STRATEGY.md` close it: **L3** searches the schedule space, **L2** holds every engine to one written contract.

- [feat] **Part A — `src/model.property.spec.ts`: stateful property-based model testing.** `fast-check`'s `fc.commands` generates random sequences over the whole op surface — `set` / `setMany` / `remove` / `evict` / `retain` / `release` / `gc` / `tx.set` / `tx.commit` / `tx.rollback` / `flush` / `dispose+reboot` — plus **IO-completion order as a generated dimension** (a gated `writeBatch`, the `inflight-overlay.spec.ts` idiom), and runs each against a reference model. Agreement is asserted after **every** command and again after every simulated reload. The leverage is that the intended meaning is one line — *a Map that survives reloads* — so the oracle is nearly free.
- [feat] **The oracle is three plain Maps: `mem`, `disk`, `refs`.** Its simplicity is load-bearing and is called out in the file header as a reviewer obligation, because two copies of the same logic agree with each other even when both are wrong. It encodes only the **contract** — ADR-004's evict-keeps-the-row / remove-deletes-it split, write-behind durability, optimistic writes not reaching disk until commit — never the implementation. Three things are deliberately *not* modelled, each with a stated reason rather than a silent approximation: concurrent transactions (would need a second copy of `transactions.ts`'s replay algorithm → L5), manifest-mode retention (the coordinator becomes a second author of `refs`), and plain writes onto a key an open transaction already touched (guarded by each command's `check()`).
- [feat] **The DAN-647 interleaving is pinned as an always-executed example, not left to the sampler.** `fc.assert(..., { examples: [...] })` forces the exact shape of the dispose-during-in-flight-flush data loss the example suite missed: hold IO → `set(X)` → `flush()` parks on the held `writeBatch` → `set(Y)` enters the dirty set → `dispose()` → release → is `Y` durable? Before ADR-017, `Y` was silently dropped.
- [feat] **A negative control, because a suite only ever observed passing is indistinguishable from one that asserts nothing.** A fault-injection seam silently swallows one confirmed put on its way to the engine — the DAN-647 *symptom* — and the test requires the property to **reject**, then asserts the fault was actually injected. Same discipline as DAN-658's red negative control in CI: a gate is not proven by watching it pass.
- [feat] **A liveness floor on the search itself.** `numRuns` is what was *requested*, not what ran, and a generator that quietly produces empty command lists reports the same green as a thorough one. The run now counts executed commands and asserts a floor. This is not decoration — it caught a real weakness during development: the default `fc.commands` size was generating only **~4.6 commands per run** (929 total). Raising to `size: "max"` against `maxCommands: 24` more than doubled the explored space to **2107 commands**, at 44 ms.
- [feat] **Part B — `src/engine-conformance.ts` + `.spec.ts`: ONE contract, three engines.** `memoryEngine`, `idbEngine` (fake-indexeddb — the real IDB algorithms), and the **real sqlite-wasm SQL core** bound directly as an in-process engine, so every property runs against real transactions, real JSON encoding and real bind-variable limits with no worker plumbing in the way. Properties beyond the four it replaces: puts REPLACE rather than merge (merge is the *store's* semantic; an engine that also merged would double-apply it), batches larger than any statement-variable limit (1200 rows crosses SQLite's 500-bind chunking and the historic 999 cap), keys containing separators and non-ASCII ids, nested structures, and durability across a reopen.
- [feat] **Capabilities are declared and checked in BOTH directions, not inferred.** `reopen` and `versioned` are explicit. An engine claiming versioning must increment; one disclaiming it must genuinely return `undefined` — a half-implemented counter now fails. `memoryEngine` omits `reopen` and the durability block is **skipped rather than faked**, which records the honest fact that the mock is not durable. That asymmetry is the whole argument of `TESTING-STRATEGY.md`: the mock is a speed optimisation that must itself be conformance-checked.
- [fix] **The kit found a real divergence on its first run → DAN-724.** `memoryEngine` and the SQLite core both populate the optional ADR-005 per-row `version`; **`idbEngine` — the default engine — never sets it.** Contract-legal (`version?: number`), but the hazard runs *opposite* to the usual mock-hides-reality direction: the mock is too **capable**, so consumer logic reading `row.version` passes locally and silently receives `undefined` from IndexedDB in production, where it compares as "not newer" everywhere. Latent today (nothing branches on it); the exposure arrives with Stage-3 sync, which is exactly the layer that would want it. It was invisible until now because the only assertion covering it ran against `memoryEngine` alone.
- [chore] **The duplicated contract is gone from `src/engines/engines.spec.ts`.** It previously covered memory + idb only. One contract with three engines behind it is the point — a property that memory passes and idb fails *is* a mock-hides-reality bug, and that is only visible when the same assertions run against both. Adding an engine is now one line in the kit, not a second copy. Engine-*specific* tests (sqlite RPC lifecycle, packaging invariant, coordinator×engine wiring, SQL-level tests) stay where they were.
- [chore] `fast-check@^4.9.0` added as the single new dev-dependency, as the ticket sanctions. Provenance checked before install (canonical author `dubzzz`, 30M weekly downloads).
- [docs] The conformance kit is deliberately **not published** — not re-exported from `src/index.ts`, so the packed tarball and `scripts/expected-pack-manifest.txt` are unchanged. `AGENTS.md`: adding a published file is a deliberate act, and it would add a permanent compatibility promise. Tracked as DAN-725 rather than done by side effect.
- Verified: `CI=true pnpm -r test` **465 (436 core + 29 mcp), 2 skipped** — up from 437; the arithmetic is exact (+38 new, −8 from the de-duplicated contract, 2 skipped are memory's declined durability block). `pnpm -r typecheck` / `pnpm -r lint` / `pnpm -r build` green. `pnpm check:publish-surface` (9 assertions, 4 artifacts) and `pnpm check:pack-manifest` (**11 entries, unchanged, both directions**) green. The ticket's own DoR anchor script passes all of its substance, anti-vacuity and engine-coverage checks.

**Teeth, proven.** The negative control is the load-bearing one and it is committed, not a one-off: swallow a confirmed write → the property **rejects**. The liveness floor was probed by raising it to an impossible value to read the true count (2107) before setting it at 1000 for seed variance. One lint warning surfaced during the pass (`unicorn/no-useless-spread` on the model's `gc` command) and was **fixed rather than suppressed** — deleting the current entry during `Map` iteration is well-defined, the same idiom `persist.ts` relies on when releasing scope retentions.

## [2026-07-23] — DAN-658 adversarial review: the CI written to catch false greens was itself full of them — six fixes, every one an instance of "a check that reports success on a question it never evaluated" (branch `tackle/dan-658-ci-workflow`)

Consolidated fix pass from an adversarial review of the CI landed in the entry below. Six findings, each independently re-verified before implementation. The through-line is one defect class, and this repo has now hit it **five times in a single day**: `grep -c` exiting 1 on success (DAN-657); `! grep -q` no-oping under `set -e` (self-review of this branch); a lint reporting clean on a scan that never opened the file (DAN-649); an absence assertion passing over a zero-byte artifact (F3 below); and a consumer smoke test that never opened the tarball (F1 below). Every fix here is written to **fail closed**, and every one carries an executed red-before/green-after.

- [fix] **F1 / CRITICAL — `engines-floor` never touched the tarball.** The one job whose entire purpose was to prove the packed artifact runs on Node 18 ran `node "$GITHUB_WORKSPACE/scripts/smoke-consumer.mjs"` from the consumer directory. That script lives **inside** the package, and Node's **package self-reference** rule resolves a bare `import("colada-db")` through the *nearest enclosing* `package.json`'s `exports` map — based on the importing file's location, never the cwd, and without consulting any `node_modules`. So it loaded `$GITHUB_WORKSPACE/dist/index.mjs` every time; the `npm install` of the tarball was decorative. Measured: a consumer directory containing **only a bare `package.json` — no `node_modules`, nothing installed** — passed every assertion including "✔ colada-db runs on the claimed engines floor"; so did a tarball built with `files: ["LICENSE","README.md"]`. The author had reasoned carefully about the *install* location (there is a comment explaining why it must sit outside the workspace) and missed that the *script's* location is what determines resolution. Fixed by `cp`-ing the script into the consumer dir before running it, **plus** a fail-closed guard inside the script: it resolves `colada-db/package.json` through `createRequire(import.meta.url)` (Node-18-safe, unlike `import.meta.resolve`) and refuses to assert anything unless the resolved path contains a `node_modules` segment — so deleting the `cp` from the YAML later exits 1 with an explanation instead of silently testing the working tree.
- [fix] **F2 / HIGH — the `Pack` step was a printer, not a gate.** `pnpm pack … && tar -tzf … | sort` produces output and asserts nothing. Combined with F1, the `files` publish allowlist had **zero coverage in either direction**: a file that should ship but stopped shipping went green, and a file that ships but shouldn't (`.env`, `src/`, a stray fixture) went green. New `scripts/check-pack-manifest.sh` packs and `diff -u`s the manifest against `scripts/expected-pack-manifest.txt` — a human-maintained, commented allowlist of the 11 published entries — with `LC_ALL=C sort` on both sides so collation cannot differ between macOS and ubuntu. It self-tests that the comparison can fail, fails closed on zero or multiple tarballs, and its failure message says plainly that **adding a published file is a deliberate act**: a `-` line means a consumer's import is now broken, a `+` line means something is being shipped that nobody decided to ship, and a version already on the registry cannot be un-shipped. Wired into CI, `pnpm check:pack-manifest`, the pre-push hook, and `prepublishOnly`.
- [fix] **F3 / MEDIUM — both publish-surface implementations passed against a MISSING and an EMPTY artifact.** `assert_absent` is `if grep -q "$p" "$@"; then fail; fi` — and `grep` exits **2** on a missing file, not 0, so the `if` takes the false branch and the assertion "passes" over a file nobody opened. Worse, an empty file contains no forbidden substring by definition. Executed: deleting `dist/index.d.mts` printed `✔ 4 invariants`, exit 0; truncating it to zero bytes made **both** implementations green with no stderr at all — so ADR-019's framework-free-type-surface claim was being asserted vacuously against a zero-byte declaration file. The existing anti-vacuity machinery proved each *detector* could fire; nothing proved the *subject* was substantive. Both legs now floor the subject before asserting: the shell requires `-e` / `-f` / `-s` and treats any `grep` exit ≥2 as "the check did not run" rather than "no match"; the Node script adds a `SUBJECTS` table giving every artifact a byte floor **and** an anchor a healthy build always emits (`declare function createEntityStore`, `function runSqliteWorker`, …), refuses to assert over an artifact that fails either, and refuses outright if an invariant names a file with no `SUBJECTS` entry. *A working detector aimed at nothing is still nothing.*
- [fix] **F4 / MEDIUM — the `Lint` gate had no teeth for oxlint's own rules.** `oxlint` exits **0** on warnings, and with no `.oxlintrc.json` every default rule is warn-severity — so the only thing that could fail the Lint step in CI, the pre-push hook, or `prepublishOnly` was the repo-local `no-unguarded-process-env` rule. Executed: a file containing `debugger` and an unused variable produced two warnings and exit **0**. `pnpm lint` is now `oxlint --deny-warnings . && node scripts/no-unguarded-process-env.mjs` (flag confirmed present in the pinned oxlint 1.74.0). **The existing tree was already clean under the flag — nothing was suppressed, no rule severity was relaxed, and no `.oxlintrc.json` was added to buy green.** Coverage confirmed repo-wide by probing `src/`, `packages/mcp/src/` and `scripts/`.
- [fix] **F5 / MEDIUM — `dist/sqlite-worker.mjs` is a published entry point and had no gate at all.** `exports["./sqlite-worker"]` ships it, but the publish assertions covered only `dist/index.{d.mts,mjs}`, and the smoke test never imported it — so the `target: "esnext"` × Node-18 syntax risk that justifies the entire `engines-floor` job was unproven for half of what ships, and the branding invariant was blind to it. Latent today (`process.env` count is 0 in that chunk) but `src/engines/sqlite.ts` carries a guard, so a bundling change moves it into scope without anyone editing a gate file. Both entry points are now covered by the branding, optional-chaining and framework-free invariants in both implementations, and the smoke test imports `colada-db/sqlite-worker` and asserts `runSqliteWorker` is callable — after F1, so it exercises the tarball rather than the working tree.
- [docs] **F6 / MEDIUM — ADR-021 is titled "CI is the gate of record" and CI cannot currently be a gate.** Executed: `gh api repos/Danny-Devs/colada-db/branches/main/protection` → **403 "Upgrade to GitHub Pro or make this repository public"**; same for `/rulesets`. The repo is private on a plan without branch protection, so **a red CI does not block a merge** — the workflow is advisory by construction. ADR-021 gains an **Enforcement status** section stating that plainly, naming the checks that should become required once the repo goes public or the plan changes (`gate (node 20|22|24)`, `publish surface`, `engines floor (node 18 consumer)`), noting that required checks also need "require a PR before merge" to mean anything against a direct push, and recording that `[skip ci]` currently yields a commit with *no* CI record — an absence that reads like green at a glance, and which required-checks would correctly render as **pending**. The title stands; the honesty was the fix. A gate everyone believes is binding, but isn't, is the same false-green disease at the process layer. *(Out of scope, tracked separately: making `/land` refuse to merge on a red CI — that lives in another repo.)*
- [docs] LESSONS.md gains the self-reference entry — genuinely non-obvious, and it defeated a job written specifically to avoid it. ADR-021 amended in place (this branch's own unlanded ADR) with the five substantive amendments and the enforcement status. AGENTS.md documents `--deny-warnings` as load-bearing, the two publish gates, and the advisory-CI caveat.
- Verified: `CI=true pnpm -r test` **437 (408 core + 29 mcp)** — baseline unchanged, as expected for a pass that adds no source behaviour; `pnpm -r typecheck` / `pnpm -r lint` / `pnpm -r build` green; `pnpm check:publish-surface`, the shell cross-check, and `pnpm check:pack-manifest` all green.

**Teeth, proven — each fix red before green, executed rather than argued.** *F1:* bare consumer dir + script run in place → **exit 0, all assertions green** (the defect); script copied into the consumer dir + nothing installed → exit 1 `ERR_MODULE_NOT_FOUND 'colada-db'`; + the real tarball → exit 0 (11 assertions incl. the new `./sqlite-worker` pair); + a `files`-stripped tarball → exit 1; and the pre-fix arrangement now trips the in-script self-reference guard instead of passing. *F2:* `files` reduced to `["LICENSE","README.md"]` → red with 7 `-` lines; a `.stray-fixture.txt` added to `files` → red with a `+` line; clean tree → exit 0. *F3:* `dist/index.d.mts` truncated to 0 bytes → both legs exit 1 (previously both green); deleted → both exit 1 (previously the shell leg printed `✔ 4 invariants`); replaced with a 20 KB anchor-less husk that clears the byte floor → both exit 1. *F4:* `debugger` + unused var → exit 0 without the flag, exit **1** with it; clean tree → exit 0. *F5:* plugin branding injected into `dist/sqlite-worker.mjs` → both legs red; `process.env?.NODE_ENV` injected there → both legs red (previously both invisible). *F6:* the 403s above, re-run and captured. **In CI** — because F1 and F2 are defects *in the workflow*, so a local proof is not a proof of them:

| run | commit | result |
|---|---|---|
| [30054860691](https://github.com/Danny-Devs/colada-db/actions/runs/30054860691) | the fix pass (`ecc8d14`) | **all 5 jobs green.** `engines floor` log shows `v18.20.8` → `✔ package resolved from node_modules (not a package self-reference)` → 11 assertions incl. `✔ ./sqlite-worker subpath resolves and parses on this Node` → `✔ colada-db runs on the claimed engines floor (v18.20.8)`. |
| [30054969287](https://github.com/Danny-Devs/colada-db/actions/runs/30054969287) | scratch `80c24a0`, `files` stripped to `["LICENSE","README.md"]` | **`publish surface` RED and `engines floor` RED, `gate` green ×3** — a red X that names the failing gate. `publish surface` failed at the manifest diff with 7 `-` lines. `engines floor` got past `✔ package resolved from node_modules` and then died on `ERR_MODULE_NOT_FOUND .../floor/node_modules/colada-db/dist/sqlite-worker.mjs` — a `node_modules` path, i.e. it read the tarball. **This is the exact package shape that went green before the fix.** |
| [30055078147](https://github.com/Danny-Devs/colada-db/actions/runs/30055078147) | revert `f34a5e3` + this entry (`a0cc91b`) | back to all 5 jobs green. |

## [2026-07-23] — DAN-658: the repo had no CI, so every "durable encoding" shipped this week fired only when a human typed the command (branch `tackle/dan-658-ci-workflow`)

The multiplier ticket on the whole 2026-07-22/23 durability family. The house doctrine ranks encodings **lint > test > skill > LESSONS.md** on the reasoning that a lint "fires on every CI run and therefore cannot be ignored" — and there was no CI here, so the top two rungs of that ladder were aspirational. 437 tests, the `no-unguarded-process-env` rule, the DAN-647/648 durability pins and the ADR-019 type-surface gate all ran on human memory alone.

- [feat] **`.github/workflows/ci.yml` — CI is now the gate of record (ADR-021).** On every push and PR. Three independent jobs so a red X names itself: `gate` runs the four Definition-of-Done commands (`CI=true pnpm -r test` → `pnpm -r typecheck` → `pnpm -r lint` → `pnpm -r build`) as separate named steps across Node 20/22/24 with `fail-fast: false`; `publish-surface` asserts the built artifact's textual invariants and packs the tarball; `engines-floor` verifies the consumer promise. Concurrency cancels superseded runs on branches but **not** on `main`, whose runs are the historical record of what was green when.
- [feat] **`scripts/check-publish-surface.mjs` — the artifact invariants, asserted.** Four detectors over `dist/`: no `pinia-colada-plugin-normalizer` branding in the shipped ESM or declarations, the literal strippable `process.env.NODE_ENV` present, no `process.env?.NODE_ENV` optional chaining (the exact DAN-649 regression), no `@vue/reactivity` in the declarations (ADR-019). Written as a script rather than three `grep` lines in the workflow for a deliberate reason: *an assertion that only fires in CI is the same disease as one that only fires when a human types the command, relocated*. It runs from `pnpm check:publish-surface`, the pre-push hook, `prepublishOnly`, and CI — one implementation, four trigger points. Exit codes are explicit and never inferred from a match count, so it cannot reproduce the DAN-657 trap where `grep -c` exits 1 on zero matches and a PASS reads as a FAIL.
- [feat] **Every publish-surface detector self-tests before it asserts.** Direct application of LESSONS.md 2026-07-23 ("a gate that manufactures confidence it has not earned is worse than no gate"). Each invariant is run against a synthetic counter-example *first*; a detector that fails to fire on its own counter-example is reported as a **vacuous gate** and the script refuses to assert against `dist/` at all. The workflow additionally cross-checks with raw `grep` against hardcoded paths — not redundant, since the positive controls prove each *detector* can fire but cannot catch the script reading a wrong-yet-existing path, under which controls and assertions would both pass vacuously.
- [feat] **`engines-floor` job — `engines.node: ">=18"` is now tested rather than asserted.** It never had been. The finding that forced the design: the **dev toolchain floor is Node ≥20.19** (`tsdown@0.21` `>=20.19.0`; `oxlint@1.74`, `vite@8.1.5`, `oxfmt` all `^20.19.0 || >=22.12.0`), so a matrix that naively included Node 18 cannot install, let alone build. Those are two different promises to two different audiences — contributors vs consumers — and one matrix axis cannot express both. The job builds on Node 22, packs, switches the runtime to Node 18 in place (second `setup-node`, no cross-job artifact round-trip), then installs the tarball plus the declared peer range **outside the workspace** and runs `scripts/smoke-consumer.mjs`: bare-specifier import through the published `exports` map, export-count floor, then a real store write/read/merge round-trip, because parsing is not running. *Measured, not assumed: verified locally on Node 18.20.8 before the workflow was written — 32 exports, entity round-trip and shallow-merge both correct. So `>=18` is accurate and was **not** narrowed to `>=20` for CI's convenience.* `target: "esnext"` in `tsdown.config.ts` makes this a live risk rather than a formality; if a toolchain bump emits syntax Node 18 cannot parse, this job goes red and the honest fixes are to lower the build target or raise `engines` on the evidence — never to delete the assertion.
- [feat] **`pnpm pack` runs on every push.** The `files` allowlist decides what a consumer actually receives; a pack that fails or silently omits `dist` is otherwise a publish-day discovery, and publish day is the one day nobody wants a surprise.
- [feat] **`.githooks/pre-push` — opt-in fast local mirror.** `pnpm hooks:install` / `pnpm hooks:uninstall`. Deliberately **not** auto-installed via a `prepare` lifecycle script: that would silently mutate a contributor's git config on `pnpm install`, and it buys little, because a hook is always bypassable with `--no-verify`. Making it *feel* authoritative while remaining bypassable is precisely the false comfort this repo was burned by once already. Event-driven and visible — no daemon, no watcher.
- [fix] **Self-review caught the cross-check asserting nothing — `set -e` is ignored for `!`-negated commands.** The raw-`grep` leg was written in the idiomatic spelling (`! grep -q "forbidden" dist/index.mjs` under `set -euo pipefail`) carried in verbatim from the ticket. POSIX disables `set -e` for `!`-inverted commands, so **three of the four lines were no-ops**: injecting the plugin branding exited 0, and injecting `process.env?.NODE_ENV` — the exact regression the line existed to catch — also exited 0. Only the bare `grep -q` gated; the last `! grep` appeared to work solely because a script's exit code is its final command's. The step had never executed against violating input (green runs on a clean tree, and on the red run an earlier step failed first), so nothing revealed it. Replaced with `scripts/cross-check-publish-surface.sh`: explicit `if ... exit 1` helpers that do not interact with `set -e`, plus a self-test running both helpers against poisoned fixtures before any real assertion. All four invariants re-verified by corrupting `dist/` four ways and confirming exit 1 each time. Encoded in LESSONS.md — same family as DAN-657: *in shell, an assertion's exit code often does not mean what it looks like it means*.
- [chore] `prepublishOnly` now also runs `pnpm check:publish-surface`; `hooks:uninstall` made idempotent (`git config --unset` exits 5 when the key is absent, so running it twice reported failure); `AGENTS.md` documents CI as the gate of record and the two Node floors; ADR-021 records the decision, the four rejected alternatives, and the `esnext`-target risk.

**Teeth, proven.** A CI config observed only passing is not verified (house rule: build-green ≠ verified). A scratch commit reintroducing `process.env?.NODE_ENV` at a guard site was pushed, observed to turn the run **red** across two independent jobs for two independent reasons — `gate` at the test step (`src/process-guard.spec.ts` strippability pin) and `publish-surface` at the assertion step — then reverted. Run URLs are recorded on DAN-658.

## [2026-07-23] — DAN-649 review gauntlet: the guard shape was safe but NOT strippable, the lint tested containment instead of dominance, and `Symbol.for` opened an encode/decode asymmetry (branch `tackle/dan-649-publish-hygiene`)

Consolidated fix pass from a three-adversary review of the DAN-649 entry below. Eleven findings, each independently re-verified before implementation. Two classes dominate: **a fix that was correct at runtime and wrong at build time**, and **a lint that manufactured confidence it had not earned**.

- [fix] **The guard shape defeated dead-code elimination — all five sites changed.** `typeof process !== "undefined" && process.env?.NODE_ENV !== "production"` is perfectly safe at runtime and every semantic test passed against it, which is exactly why it survived review. But `@rollup/plugin-replace` substitutes the **literal** member expression `process.env.NODE_ENV`; a `?.` between `env` and `NODE_ENV` leaves nothing for a literal definer to find. Mechanically confirmed on the shipped artifact: `dist/index.mjs` contained `process.env?.NODE_ENV` 5× and `process.env.NODE_ENV` **0×**. Measured consequence on the canonical Rollup production chain: **+1,106 bytes minified, +400 gzipped, all five internal dev-warning strings leaked into the production bundle**, and five constant-folded reads became live runtime reads. The shape is now `typeof process !== "undefined" && process.env && process.env.NODE_ENV !== "production"` — strips cleanly under a literal definer AND tolerates a `process` shim with no `.env` (the plain conjunct evaluates falsy rather than throwing, which the bare `process.env.NODE_ENV` variant does not). Sites: `matcher-view.ts`, `engines/sqlite.ts`, `persist.ts` ×3. *(Blast radius, measured rather than assumed: five real production toolchains were built and grepped during review. `@rollup/plugin-replace` + terser leaked; **esbuild, Vite 8, webpack 5 and Terser `global_defs` all stripped correctly** — their `define` is AST-aware and folds the `?.` form. So most consumers, including this library's Vite-first audience, were never affected. The fix stands regardless: we do not control which definer a consumer uses, so write to the strictest, not the smartest.)*
- [test] **The strippability pin — the durable half of this lesson, and the one no semantic test can reach.** `src/process-guard.spec.ts` now asserts that every extracted guard contains the literal substring `process.env.NODE_ENV`, plus a tree-wide check that no shipped source optional-chains between `process.env` and `NODE_ENV`, plus a positive control proving the detector fires on the broken shape. Without this pin the next agent "tidies" the guard back to optional chaining, every existing test stays green, and every downstream production bundle silently regresses — the two shapes are runtime-identical, so the entire existing suite is blind to the difference.
- [fix] **The lint rule tested CONTAINMENT, not DOMINANCE — false confidence, which is worse than no rule.** `isGuarded` called `subtreeHasCheck(parent.left, …)`, which walked the entire left subtree, so a `typeof` check *anywhere* in there whitelisted the read even when it did not control it. Proven: `(typeof window !== "undefined" || typeof process !== "undefined") && process.env…` passed both the lint and the companion regression, and still throws `ReferenceError` in a real browser realm (executed under `node:vm` with `window` present and `process` genuinely undeclared). Replaced with a real dominance check — recursing through `&&` only for positive guards and `||` only for negative ones (the exact dual), never through the opposite connective, a ternary, a call, or a parenthesized disjunction.
- [fix] **The lint rejected this repo's own house idiom and had no escape hatch — the two conditions that get a required gate deleted.** `src/persist.ts` guards `navigator` with an inverted early return and `src/engines/idb.ts` hoists its `indexedDB` check into a returned boolean; the rule accepted neither shape for `process`, and offered no suppression mechanism at all, so the only escape was removing it from `package.json`. Now: (a) `if (typeof process === "undefined") return|throw|continue|break;` dominates the remainder of its block; (b) simple same-file constant propagation makes `const HAS_PROCESS = typeof process !== "undefined"` a valid guard token — deliberately unscoped and non-transitive, qualifying only when the name is declared exactly once in the file, so it fails loud on ambiguity; (c) `// lint-ok: no-unguarded-process-env — <reason>` on the offending line or the one above, with a **mandatory** reason (a reasonless suppression is itself a violation, or the auditable hatch is just a mute button).
- [fix] **`globalThis.process` was invisible to both gates — and the rule's own message invited it.** `isNamePosition` exempted any identifier where `parent.name === node`, which swallowed the `process` in `globalThis.process`. The error message said "`process` is a Node global", so an engineer reaching for the portable-looking `globalThis.process.env.NODE_ENV` got a green lint and a green test — and a `TypeError: Cannot read properties of undefined` in the same place, on the same degradation path, for the same audience (executed and confirmed). Member reads off `globalThis` / `self` / `window` / `global` are now reported unless optional-chained *through* the global (`globalThis.process?.env?.NODE_ENV`) or dominated by a `typeof` / `in` check.
- [fix] **The rule's suggested-fix message prescribed the shape that breaks DCE** (2 sites: the module docstring and `MESSAGE`). The logic was fine; the *advice* steered every future fix straight into the defect above. It now prescribes the strippable form and states the reason in one sentence, because the fix hint is the highest-leverage string in the file — it is what the next author copies.
- [fix] **The CLI reported success on paths that do not exist.** `collectFiles` swallowed `statSync` ENOENT and `checkPaths` resolved args against the repo root rather than `process.cwd()`, so `node scripts/no-unguarded-process-env.mjs some-typo` printed ✓ and exited 0. Harmless for the bare `pnpm lint` invocation, fatal the moment the rule is wired into lint-staged, a pre-commit hook, or a changed-files CI step — a green tick over an empty scan. Now `console.error` + exit 2 on a missing target, with args resolved against the working directory and `DEFAULT_ROOTS` still anchored to the repo (new export: `resolveTargets`).
- [fix] **Extension allowlist and spec-exemption bypass.** `isLintable` required `extname === ".ts"` exactly, so `.tsx` / `.mts` / `.cts` shipped source was silently unscanned; widened to all four. Separately, `collectFiles` on a *file* target checked only the extension and never consulted `isLintable`, so naming a spec file directly linted it — contradicting the documented exemption, and precisely what a changed-files wiring does. File targets now route through the same gate.
- [fix] **`ENTITY_REF_MARKER` is now `Symbol.for("colada-db/entity-ref@1")`** — versioned. The unversioned registry key correctly made two copies of colada-db agree on ref identity (that benefit is real and was executed), but a global registry interns across **versions** as well as instances: a page with v1 + v2 would have v1's `isEntityRef` accept a v2 ref. The plain `Symbol()` it replaced degraded cross-version refs safely to opaque data, and that safety property was being traded away silently. The suffix keeps 100% of the duplicate-instance benefit within a major and restores fail-safe degradation across them. Bump it on any breaking change to the ref shape.
- [fix] **Closed the encode/decode asymmetry — this is what makes the versioned symbol safe.** The decode path validated the full ref shape with type checks (DAN-648/M2) while the encode path and `isEntityRef` checked only `record[ENTITY_REF_MARKER] === true`: the write path trusted exactly what the read path distrusted. A per-instance symbol had made that latent — a foreign-shaped ref was structurally unable to reach the encode branch. `Symbol.for` interns globally, so for the first time a ref minted by another copy satisfies the marker check, and an unvalidated encode destructively rewrites it into a malformed `__cdb_ref` row that decode's own M2 guard then permanently refuses — an unrecoverable relationship where pre-change it round-tripped intact. Both sites now mirror the M2 guard (`entityType`/`id`/`key` all `string`). Verified non-destructive; legitimate refs still resolve. **Severity, honestly: this requires two colada-db copies with *different* ref shapes on one page, i.e. a future major. The package is `0.1.0`, private, zero users on disk. A future hazard closed while it is free — not a live bug.**
- [feat] **`prepublishOnly": "pnpm build && pnpm typecheck && pnpm test && pnpm lint"`.** `dist/` is gitignored, `files` ships `dist`, and there was no `prepublishOnly` or `prepack` — so `npm publish` would ship whatever `dist/` happened to be sitting on disk, and a stale build from before this branch would publish silently and green. On a ticket whose entire premise is "the shipped artifact must behave correctly", the artifact was not tied to the committed source.
- [docs] **Two factually-wrong claims in `src/types.ts` corrected.** (1) The `Symbol.for` docstring justified itself by the chip-3 case where the Pinia Colada adapter carries its own engine copy. That is false — the frozen copy declares `Symbol("pinia-colada-entity-ref")`, a plain symbol with a *different description*, which `Symbol.for` cannot intern with. Reworded to the case actually closed (two `colada-db` copies deduped apart by npm — version skew, which chip 3's dependency swap makes possible), and states plainly that closing the frozen-copy case would need the plugin to adopt the same registry key. (2) "Changing this symbol therefore cannot affect any persisted data" was imprecise: the symbol value is never serialized, but symbol *identity* is the branch condition in `encodeEntityRefs`, so it determines disk **content**. Reworded — the value is never serialized, it gates encoding, single-instance emitted bytes are unchanged (verified byte-identical), so no migration is required.
- [docs] ADR-020 (`docs/adr/020-ref-identity-is-a-versioned-global-registry-key.md`) — the ref-identity contract, the version-suffix rule, and why symmetric validation is the thing that makes a global registry key safe. LESSONS.md gains two entries: the strippability lesson (an environment guard that is runtime-correct can still be build-time wrong, and no semantic test can tell) and the containment-vs-dominance lesson (a lint that tests containment manufactures false confidence, which is worse than no lint).
- [test] `src/process-guard.spec.ts` 30 → 61, `src/encode-decode-integrity.spec.ts` 13 → 22. Every new lint capability carries a fixture AND a firing proof: the pre-gauntlet rule and the post-gauntlet rule were run side by side over 13 shapes — **13/13 land on the intended verdict, 8/13 are verdict CHANGES** (3a/3b/3c: REJECT → ACCEPT for the house idioms; 4/5: ACCEPT → REJECT for the shapes that crash). FIX 6/7 proved at the CLI: a typo'd path was `exit 0` and is now `exit 2`; a cwd-relative arg was silently `exit 0` and now finds 14 violations; `.mts`/`.tsx` fixtures went `exit 0` (never opened) → `exit 1`; a directly-named spec file went linted → exempt.
- Verified: `CI=true pnpm -r test` **433 (404 core, +40; 29 mcp unchanged)** from the 393 baseline, `pnpm -r typecheck` / `pnpm -r lint` / `pnpm -r build` all green. Post-build gates: `! grep -q "pinia-colada-plugin-normalizer" dist/index.d.mts dist/index.mjs` ✓, `grep -q "process\.env\.NODE_ENV" dist/index.mjs` ✓ (5 occurrences), `! grep -q "process\.env?\.NODE_ENV" dist/index.mjs` ✓ (0). **Observed running:** the shipped `dist/index.mjs` was bundled and executed in a `node:vm` realm with NO `process` binding at all and driven through three real degradation paths (engine-open failure, writeBatch failure, foreign StoreBoundary) — 3/3 graceful, 0 warnings. Negative control: reverting the guards to the pre-fix shape inside the same bundle dies with `ReferenceError: process is not defined`, so the harness demonstrably detects what it is looking for. Strippability reproduced end-to-end with a literal definer: the literal form folds and drops the warning string, the `?.` form leaves it in the minified output.

## [2026-07-23] — DAN-649: pre-publish hygiene — `process` no longer crashes bundler-less runtimes (+ a lint that keeps it that way), stale branding out of the shipped artifact (branch `tackle/dan-649-publish-hygiene`)

Publish-quality bundle from the 2026-07-22 audit, scoped to A1/A3/A5/A6. A4 was struck (shipped by DAN-654) and A2 split out to DAN-656 — no public read-API type was touched here. No data-loss surface; no engine protocol, matcher, or transaction *logic* changed (docstring-only edits to those files).

- [fix] **A1 — `process.env.NODE_ENV` crashed bundler-less ESM on every degradation path.** All five sites now guard with `typeof process !== "undefined" && process.env?.NODE_ENV !== "production"`: `matcher-view.ts` (foreign-boundary retention warning), `engines/sqlite.ts` (OPFS-unavailable), and three in `persist.ts` (writeBatch failure, format-version-too-new, boot/engine-unavailable). `process` is a Node global; a CDN / Deno / plain-`<script type=module>` consumer — the ADR-008 §4/§5 framework-free audience — has no such binding. The happy path never touched it, so those consumers worked fine right up until persistence degraded, at which point graceful degradation became `ReferenceError: process is not defined`. Bundler users were fully shielded (static replacement), which is exactly why it survived this long. The chosen shape still folds to `false` under a bundler's `process.env.NODE_ENV` replacement, so dead-code elimination of the dev warnings is preserved.
- [feat] **A1 durable encoding — `no-unguarded-process-env` lint rule** (`scripts/no-unguarded-process-env.mjs`), wired into `pnpm lint` so it gates every run. AST-based (TypeScript compiler API, already a devDependency — no new install): it bans any reference to the global `process` binding in shipped source unless dominated by a `typeof process !== "undefined"` guard, understanding `&&` / `||` / ternary / `if` guard positions, local shadowing, and name-vs-value positions. Spec files are exempt (Node-only, never published). The error message teaches the *reason* (bundler-less consumers crash on degradation paths), not just the rule name — the lint is the thing that stops a sixth site being added next month.
- [test] `src/process-guard.spec.ts` (30) — the regression evaluates the **shipped guard shape** rather than merely asserting a warning fires. It extracts each real guard expression from source via AST and evaluates it with `process` bound to `undefined`, asserting no throw, warning suppressed, and — as the anti-cheat — that the guard still returns `true` under Node/bundler dev and `false` in production, so "fix" can't mean "delete the warning". A positive control proves the pre-fix shape genuinely throws under the same harness. Verified failing pre-fix: reverting one site turned exactly that site's two assertions red while the other four stayed green, and the lint independently flagged the same line.
- [fix] **A3 — stale `pinia-colada-plugin-normalizer` branding in the shipped artifact: 17 refs in `src` → 1.** The headline defect was a **wrong import path**: JSDoc documented `pinia-colada-plugin-normalizer/sqlite-worker` when the real export is `colada-db/sqlite-worker` (`package.json` `exports`), so a consumer copy-pasting from an IDE tooltip got module-not-found (`sqlite-worker.ts`, `engines/sqlite.ts`). Also corrected: `@module` tags (`types.ts`, `persist.ts`, `pagination.ts` — the last also claimed a `/pagination` subpath that has never existed; it is re-exported from the barrel) and `@example` imports (`pagination.ts` ×4, `persist.ts`). `types.ts`'s header still described the package as a "Pinia Colada plugin"; it now describes colada-db. **Deliberately preserved:** `index.ts`'s reference to `pinia-colada-plugin-normalizer` as the Vue adapter — that names a real, separate package and is correct (it does not reach `dist`). Three *historical provenance* comments (`coalesce.ts`, `normalize.ts`, `transactions.ts`) were reworded to name the plugin descriptively rather than deleted: the extraction history is true and worth keeping, but one of them leaked the stale npm specifier into consumer IDE tooltips.
- [fix] **A5 — `ENTITY_REF_MARKER` is now `Symbol.for("colada-db/entity-ref")`** (was `Symbol("pinia-colada-entity-ref")`). A plain `Symbol()` is per-module-instance, so two copies of colada-db in one page disagree on ref identity and refs silently degrade to opaque data — not hypothetical, since chip 3 has the Pinia Colada adapter carrying its own engine copy alongside an app-level `colada-db`. The stale description string (explicitly left to this ticket by DAN-654) dies with the same change. In-memory identity only — the wire/disk format uses the string key `__cdb_ref` (ADR-018), so no persisted data is affected. The false `@internal` tag was dropped: the symbol was already exported from the barrel and already present in `dist/index.d.mts`, so the tag was simply inaccurate; the formal public-vs-internal *classification* is left to DAN-656, which owns that surface.
- [docs] **A6 — `EntityKey` branding deliberately DEFERRED, with the hazard documented at the declaration.** Measured rather than assumed: it does *not* ripple into the matcher (0 references there, contrary to the ticket's guess), but `EntityKey` is load-bearing in the `StorageEngine` port contract (`loadAll` / `loadMany` / `writeBatch`), so branding it is a breaking change for every third-party engine implementation (ADR-006 / ADR-008 §2) and also crosses `engines/` and `transactions.ts` — both out of scope here. It needs its own ADR and gauntlet, the same reasoning that split DAN-656 out. The `types.ts` docstring now records the concrete failure mode (type names containing a colon round-trip wrong through the 8 first-colon split sites) and the interim contract: entity type names must not contain a colon.
- Verified: `CI=true pnpm -r test` **393 (364 core, +30; 29 mcp unchanged)** from a 363 baseline, `pnpm -r typecheck` / `pnpm -r lint` / `pnpm -r build` all green. Post-build gates: `grep -c "pinia-colada-plugin-normalizer" dist/index.d.mts dist/index.mjs` → **0 0** (also 0 in `dist/sqlite-worker.*` and `packages/mcp/dist`), and every `process.env` in `src` guarded. **Observed running:** the shipped `dist/index.mjs` was loaded with the `process` global deleted and driven through three real degradation paths (engine-open failure, writeBatch failure, foreign boundary) — 5/5 graceful, 0 warnings emitted. The same harness against the pre-fix `dist` dies with `ReferenceError: process is not defined`.
## [2026-07-23] — DAN-656: colada-db owns its public read type — `ColadaRef` ends the framework leak on line 1 of the published `.d.mts` (branch `tackle/DAN-656-coladaref`)

Pure type-surface change, **zero runtime delta** (proved: the emitted `dist/index.mjs` and `dist/sqlite-worker.mjs` are byte-for-byte identical to a build of `origin/main` — same SHA-256 on both files). `dist/index.d.mts` line 1 was `import { ComputedRef, ShallowRef } from "@vue/reactivity"`, so the primary read API of a self-described **framework-agnostic** engine spoke a framework's types at the exact layer every consumer's IDE reads first. Not a dependency bug — the signal engine is already the runtime core by deliberate choice (ADR-008 §3) — but an **ownership** bug: the read type was not ours to evolve, and it silently pre-committed the ADR-008 §3 signal-engine swap that ADR calls "a non-event."

- [feat] `ColadaRef<T> { readonly value: T }` in `src/types.ts`, barrel-exported. `EntityStore.get()` and `getByType()` (both overloads each) now return `ColadaRef` instead of `ShallowRef`/`ComputedRef`. A **widening of the declared type only** — the objects handed out are the same live `shallowRef`/`computed` instances, which satisfy `ColadaRef` structurally. `value` is `readonly` because the read API hands out a *view*: writing through the ref would rewrite the projection without emitting an event. **No setter variant added** — no write-facing surface hands out a ref (every mutation is a method taking plain data; `update()` covers read-modify-write), so `MutableColadaRef` would be a type with no inhabitant.
- [refactor] `src/store.ts` — the Vue fast path (ADR-008 §3: the adapter may share the store's ACTUAL refs) is now pinned by the compiler. An `@internal`, **non-barrel-exported** `RefBackedEntityStore` (= `EntityStore` with `get`/`getByType` narrowed back to `ShallowRef`/`ComputedRef`) annotates the implementation object, so if those methods ever stopped handing out real engine refs, `store.ts` stops compiling. It stays internal deliberately: exporting it would put the framework import straight back on line 1. All three hunks in this file are type-only (an `import type` reformat, an erased `interface`, one `const` annotation) — hence the identical bundle hashes.
- [test] `src/public-read-type.spec.ts` (4 runtime + compile-time fixtures). The compile-time pin asserts a **plain, framework-free object literal** is assignable to `ReturnType<EntityStore["get"]>` — only possible while the type is structural and unbranded, since `Ref` declares `[RefSymbol]: true`. Runtime tests assert the widening did not change the value: `get()` still returns a real `isRef` box, still the *same* box on repeat calls (no wrapper introduced), still tracks/triggers through a `computed`; `getByType()` still live and memoized; and a hand-rolled `ColadaRef` needs no framework machinery.
- [feat] `scripts/check-public-types.mjs`, wired into `pnpm build` — greps the emitted `dist/index.d.mts` for `@vue/reactivity`/`@vue/shared` and exits 1 on any hit. Catches the regression class the compile-time pin cannot: a leak arriving through some *other* newly barrel-exported type, from a file nobody edited. Type-surface only — `dist/index.mjs` importing the engine at runtime is correct and expected.
- [docs] ADR-019 (`docs/adr/019-colada-owns-its-public-read-type.md`). Records the decision, what we are **not** doing (not writing a reactivity system, not adding a reactivity port speculatively, not adding a `colada-db/vue` subpath entry), and the peer-dependency call. Names the one sharp edge explicitly: Vue's template auto-unwrap is runtime `isRef()` but its *typing* keys off the brand, so an engine ref passed raw through `setup()` unwraps at runtime while the template's types no longer expect it — the seam chip-3's adapter should own.
- **Peer-dependency call: `@vue/reactivity` stays a REQUIRED peer (`>=3.3.0`)** — the discretion granted by the ticket, exercised as a deliberate no. Removing the type import makes optional-peer *eligible*, not *correct*: `dist/index.mjs` line 1 is a top-level unconditional **value** import (`neverBundle` in `tsdown.config.ts`), not a lazy path, so marking it optional would tell package managers it is fine to omit and hand the omitter a module-resolution crash at import time. That is the distinction against `@sqlite.org/sqlite-wasm`, which is legitimately optional because it is reachable only through `sqliteEngine()`. The move becomes correct only if the signal engine goes behind a real port with a dependency-free default — a separate decision with real runtime consequences.
- **Both pins verified by executed negative control**, not by assertion: reverting `get()` to `ShallowRef` produced `TS2322: Property '[RefSymbol]' is missing in type '{ readonly value: EntityRecord | undefined; }'` (plus an unused-`@ts-expect-error` error, since `ShallowRef.value` is writable) and the build gate printed `✗ ADR-019 violation: "@vue/reactivity" appears in dist/index.d.mts (1 occurrence)` with exit 1. Then reverted.
- Verified: `CI=true pnpm -r test` **367 (338 core, +4 from a 334 baseline; 29 mcp unchanged)**, `pnpm -r typecheck` / `pnpm -r lint` / `pnpm -r build` all green, and the ticket's assertion `grep -c "@vue/reactivity" dist/index.d.mts` → **0** (baseline was 1). Scope note: `engines/`, `sqlite-worker.ts`, `matcher`/`matcher-view`, `transactions.ts`, `persist.ts` untouched.

## [2026-07-23] — DAN-654: unify the persisted format on `cdb` + reserve a `formatVersion` escape hatch (branch `tackle/dan-654-cdb-format-rename`)

Clean break, no dual-read — resolves the arch-review C4 heritage-branding collision (AGENTS.md chip 3). colada-db is `"private": true` (zero DBs in the wild) and the plugin never shipped persisted data, so the on-disk/wire rename is free NOW and a breaking migration if deferred past publish.

- [refactor] every `pcn` identifier in a wire/disk position → `cdb`: the entity-ref wire key `__pcn_ref` → `__cdb_ref` (`store.ts`), the default IDB db name `pcn_entities` → `cdb_entities` (`persist.ts`, `idb.ts`), the default OPFS file `pcn_entities.sqlite3` → `cdb_entities.sqlite3` (`sqlite.ts`, `persist.ts`). JSDoc `@default` tags and the `[pcn-persist]` diagnostic log prefixes were groomed to match. **Out of scope (deliberate):** the `ENTITY_REF_MARKER` Symbol *description* (`Symbol("pinia-colada-entity-ref")`) — in-memory identity only, never serialized, owned by DAN-649. No engine protocol/message semantics, matcher, or transactions touched.
- [feat] `CDB_FORMAT_VERSION = 1` (exported from `persist.ts`), written into the manifest INDEX row (`__cdb_manifest__:__index__`) — the one row the coordinator owns and reads on every boot, so no new key and no per-entity overhead. `ManifestIndexRow` gains an optional `formatVersion?: number`, stamped on every index-row flush. Boot policy (ADR-018, the ONLY machinery by design — NO migration, NO dual-read): **absent** → treat as v1 silently (pre-publish DBs, and any manifest-free DB, have no index row); **equal** → normal; **higher than this build knows** → `console.warn` once and proceed (forward-tolerant, never crash). Purely a future escape hatch.
- [test] `src/format-version.spec.ts` (7) — the on-disk CONTRACT round-trip: write with the new format → boot a FRESH store off the same durable bytes → all entities + manifests hydrate, on BOTH the memory engine (bytes via `snapshot()`) and the IDB path (bytes read by opening the raw object store through fake-indexeddb). Asserts the ref serializes under `__cdb_ref` at the actual bytes (exact-key-set, so the test never itself spells the retired token and trips the rename grep gate), the default IDB db is `cdb_entities`, and `formatVersion` is present + read back. ADR-018 policy pinned: absent→v1-silent, manifest-free-db→boots, higher→warns exactly once + still hydrates + no throw. Existing format tests updated deliberately (they encode the format, which changed): `encode-decode-integrity.spec.ts` (DAN-648, all `__pcn_ref` fixtures+assertions → `__cdb_ref`) and `persist.spec.ts` (default-db cleanup list `pcn_entities` → `cdb_entities`).
- [docs] ADR-018 (`docs/adr/018-cdb-format-identifiers-and-format-version.md`) records the clean-break decision + the `formatVersion` contract (absent/equal/higher semantics, no-migration-by-design). AGENTS.md chip 3 updated: the C4 collision is RESOLVED. LESSONS.md entry (a rename gate that greps for the retired token trips on tests asserting its absence — assert the exact key set instead).
- Verified: `CI=true pnpm -r test` 363 (334 core, +7; 29 mcp unchanged) from a 356 baseline, `pnpm -r typecheck` / `pnpm -r lint` / `pnpm -r build` all green, and the rename gate `! grep -rn "__pcn_ref\|pcn_entities" src/` clean.

## [2026-07-22] — DAN-647: dispose() racing an in-flight flush no longer silently drops the post-drain residue (branch `tackle/dan-647-dispose-flush-lostwrite`)

- [fix] `persist.ts` — the one true data-loss bug in the dispose seam (spec-then-fix on the durability seam; RE-VET confirmed pre-existing at `343666e` with an executed gated-`writeBatch` repro, both `set` and `remove` flavors). `dispose()` ran `finalFlush = flush()` then `disposed = true`. When a prior flush was in flight (parked in `await engine.writeBatch`), the final `flush()` took the `if (flushing) { await inflightFlush; return flush(); }` path — and the re-entrant `flush()` then bailed on `if (!opened || disabled || disposed) return` now that `disposed` had flipped true. The in-flight batch's tail recovery `if (dirtySaves.size>0) scheduleFlush()` was defeated by the SAME flag (`scheduleFlush` short-circuits on `disposed`). A `set(Y)`/`remove(Y)` that entered the dirty sets AFTER the prior drain parked was silently non-durable — no error. Only real async engines expose it (a synchronous engine resolves `writeBatch` before dispose runs, so `flushing` is already false); same "world held still across an async gap" family as ADR-012–016, the gap here being the in-flight batch and the shifted state being `disposed` itself.
- [feat] ADR-017 semantics: **the dispose final flush survives its own `disposed` guard.** `flush` gains an internal `final` param (default `false`); the guard becomes `(disposed && !final)` and the re-entrant call propagates it (`return flush(final)`). `dispose()` calls `flush(true)`; every other caller (public handle, debounce timer, gc-sweep flush-first, `visibilitychange`/`beforeunload` listeners) calls `flush()`. `disposed` is still set SYNCHRONOUSLY in dispose — so every boot/hydration post-`await` `disposed` re-check keeps firing exactly as before (the alternative, deferring `disposed` until the flush resolves, was rejected precisely because it reopens those boot guards for a dispose racing boot: retention/hydration could outlive the coordinator). The public handle exposes `flush: () => flush()`, making `final` unreachable from outside the module — so a post-dispose external `flush()` still no-ops under `disposed` and a post-dispose `set` can't sneak a batch in (`unsub()` + the subscriber's `disposed` guard). Single-write per key (drain is a move; dirty/in-flight sets are disjoint per key). Alternatives weighed and rejected in the ADR: defer-`disposed` (reopens boot guards), a module-level `finalizing` flag (needs an un-set point or a post-dispose external flush bypasses the guard — the param sidesteps the fuzzy lifetime), disposed-immune tail `scheduleFlush()` (re-introduces a post-teardown timer dispose deliberately clears).
- [test] `src/dispose-flush.spec.ts` (4) — gated-`writeBatch` `slowEngine`: set-in-flight residue durable across dispose + remove-in-flight residue durable across dispose (**three verified FAILING pre-fix**: `contact:2` dropped on the save leg, its delete dropped on the remove leg, and — because the drop also breaks it — the single-write case) + one contract pin (a `set` arriving AFTER dispose never reaches disk and the external `flush()` is a no-op — no post-dispose sneak-in, exactly one batch runs). The single-write property (final flush writes the residue key exactly once — no double-flush) is asserted on the post-fix tree.
- [docs] ADR-017 (`docs/adr/017-dispose-final-flush-survives-the-disposed-guard.md`, amends ADR-014's "dispose racing boot/unload MUST flush acknowledged dirt" boundary — the flush that boundary depends on was itself lossy under the in-flight interleaving); LESSONS.md entry (a lifecycle flag that stops NEW work must not also stop the in-flight work's own completion — exempt the final drain, don't defer the flag); `flush` and `dispose` doc comments now state the honored contract.
- [review] Self-review sweep: post-dispose external set rejected (test 4 — `unsub()` detaches the subscriber AND `disposed` guards it; external `flush()` no-ops, zero extra batches); no double-flush (test 3 — per-key writeBatch touch-count == 1; drain-as-move keeps dirty/in-flight disjoint); dispose idempotent (second dispose returns at `if (disposed) return` — unchanged, durability.spec pin green); dispose-during-boot still guarded (no boot-path code touched, `disposed` still synchronous, all post-`await` re-checks fire — boot-hydration-writes.spec green); dispose with NO flush in flight (fastEngine `dispose() performs a final flush` pin green); dispose when disabled (`finalFlush = opened && !disabled ? … : Promise.resolve()` — no flush attempted); re-entrancy termination (post-dispose no new external batch starts and the gc timer is cleared, so the final chain drains the residue once and the next `flush(true)` sees `size 0` → returns — no infinite loop); in-flight-batch-fails leg (re-entrant `flush(true)` bails on `disabled`, fail-closed — engine dead, memory already sole copy).
- Verified: `CI=true pnpm -r test` 343/343 (310 → 314 core, +4; 29 mcp unchanged), `pnpm -r typecheck` / `pnpm -r lint` / `pnpm -r build` all green. Scope note: engines/, sqlite-worker.ts, matcher/matcher-view, transactions.ts untouched — the entire fix is the `final` param (guard + re-entrant propagation), the `flush(true)` dispose call, the sealed public `flush` wrapper, and comment/doc hunks in `src/persist.ts`.
## [2026-07-22] — DAN-648: encode/decodeEntityRefs serialization integrity — `__proto__` no longer corrupts the ref-rebuild path, decode validates the full ref shape, `undefined` divergence documented (branch `tackle/dan-648-encode-decode-integrity`)

Three findings from the DAN-648 adversarial sweep over `store.ts`'s wire codec, each independently re-vetted with a failing repro before fixing. All three reproduced (no false positives). New spec: `src/encode-decode-integrity.spec.ts` (13 tests).

- [fix] **M1 (data-integrity + prototype-pollution-adjacent):** on the ref-REBUILD path of BOTH `encodeEntityRefs` and `decodeEntityRefs`, `result[key] = value` invoked the `__proto__` setter when an object carried an OWN enumerable `__proto__` key (JSON.parse / persisted data produces such keys) alongside a sibling `EntityRef`. Effect: the `__proto__` field was silently dropped AND the rebuilt object began inheriting from the supplied payload (its prototype was reassigned). The no-sibling-ref short-circuit (identity return) was never affected — confirmed. **Fix:** a shared `assignOwn(target, key, value)` helper that uses `Object.defineProperty` for `key === "__proto__"` (bypassing the setter, writing a real own enumerable data property, leaving `Object.prototype` intact) and plain assignment otherwise. `constructor`/`prototype` needed no special handling — they are data properties on `Object.prototype`, so plain assignment already shadows them as own props with no side effects (pinned by a passing regression test). Covers nested objects and array elements (recursion routes both through the same rebuild).
- [fix] **M2 (data-integrity):** the in-memory ref marker is a `Symbol`, but the wire format uses the plain string key `__pcn_ref`, reintroducing exactly the API-data collision the Symbol was chosen to avoid (Issue #13). The decode guard checked only `__pcn_ref===true && "entityType" in record && "key" in record` — it did NOT require `id`, nor validate types — so `{__pcn_ref:true, entityType:123, key:[…]}` hydrated a broken ref with `id: undefined`. **Fix:** require the full ref shape with correct types (`entityType`/`id`/`key` all `string`) before treating as a ref; non-conforming `__pcn_ref`-shaped data passes through as PLAIN DATA instead of a dangling ref. A collision whose shape AND types exactly match a real ref remains indistinguishable — an inherent limit of a string-keyed wire marker, bounded honestly in a code comment.
- [docs] **M3 (advisory, documented not fixed):** `undefined`-valued fields are preserved by the ref encoder, then diverge at the engine boundary — idbEngine (structured-clone) keeps an explicit `undefined`, sqliteEngine (JSON.stringify) drops the key. On read the field is `undefined` either way (absent ≡ explicit-undefined for property access), so this is not corruption; only `key in entity` / `Object.keys(entity)` differ. Chose DOCUMENT over fix: "making it uniform" would change the IDB path's observable structured-clone output (a semantic `undefined ≡ absent` decision that belongs in the storage contract, not silently in encode), and the practical blast radius is nil (nothing in the read projection depends on key presence). Added to the `StorageEngine` serialization-divergence envelope in `types.ts` and pinned with a test.

Gates: `CI=true pnpm -r test` (352 total = 323 core + 29 mcp, +13 new; all green — 314 core baseline after DAN-647), `pnpm -r typecheck`, `pnpm -r lint`, `pnpm -r build` — all green. No engines/, protocol, or matcher code touched. No new deps.

## [2026-07-21] — DAN-635: the optimistic mask now covers PUTs — an evicted optimistic put can no longer be paged over by a stale engine row (branch `tackle/dan-635-tx-foreign-interference`)

- [fix] `persist.ts` — closes flavor A of the tx-vs-foreign-writes family that ADR-015 filed as the follow-up (spec-then-fix on the durability seam; verified pre-existing at `71fdf82`, byte-identical with/without ADR-015's overlay). The transaction layer assumes a tx-touched key is not concurrently evicted or non-tx-mutated mid-flight; that assumption was unenforced and silent. **Flavor A (the must-fix — divergence SURVIVES settlement):** `tx.set(c1,"kept")` → `evict(c1)` mid-tx → `hydrateScope` pages the stale pre-tx engine row `v1` over the optimistic projection (fresh-wins via `store.has` protected the optimistic put ONLY while it stayed resident — eviction removed it, and the delete-only ADR-015 mask did not fire for a PUT). After commit, "kept" graduated to disk while memory kept `v1`: **memory `v1` / disk "kept" for the rest of the session.**
- [feat] ADR-016 semantics: **the optimistic mask covers any buffered op, not just deletes.** `hydrateRow`'s mask changes from `buffer.get(key)?.op === "delete"` to `buffer.has(key)` (one predicate), keeping mask-first precedence. A buffered PUT now vetoes hydration of its evicted key exactly as a buffered DELETE vetoes un-deletion — so a stale engine row can never page over an evicted optimistic put (flavor A dead by construction). After eviction memory is legitimately absent (ADR-004 cold row); on commit the tx op graduates and the next hydrate pages the committed value → **memory converges to the same truth disk converges to, by settlement.** No new state, no store coupling, no commit-contract change, no serverTruth touch — the change lives entirely in the read-side overlay and cannot affect the write path, the sync outbox (ADR-006 §1), or the flush pipeline.
- [feat] **Flavor B (foreign confirmed non-tx write racing a tx-touched key) is bounded, not fixed — deliberately.** The mask stays mask-first: a foreign confirmed write must NOT override it, because preferring it (candidate d) would diverge at COMMIT (the tx op graduates to disk while memory keeps the foreign value). What remains is the rollback clobber — the tx `serverTruth` snapshot goes stale the instant a foreign confirmed write lands, and rollback restores the stale snapshot over it (memory reverts to pre-tx `v1`, disk keeps the confirmed `v2`). That is a serverTruth-staleness hazard owned by the sync coordinator's server-authoritative rebase (ADR-006 §6), byte-identical pre/post this ADR, now NAMED and pinned rather than silent. Alternatives weighed and rejected in the ADR: mask-yields-to-confirmed (breaks commit-coherence — the flavor-B commit-leg pin encodes exactly this), evict-immunity (manual `store.evict` bypasses refcounts so it doesn't fix the repro; couples store to transactions; tx-holds-memory-hostage risk), settlement reconciliation (commit is event-silent by design — re-applying means new events + a subscriber loop, or forbidden silent memory correction; and it only covers commit, not the mid-tx window), and serverTruth-refresh-on-foreign-write (widens the tx conflict model into the frozen sync-outbox machinery ahead of the coordinator that owns it).
- [test] `src/tx-foreign-interference.spec.ts` (5) — flavor A with a mid-tx remount (the stale row must not resurrect; memory then converges to "kept" across commit+reboot) + no-remount pin (evict-then-commit converges cleanly) + flavor-A rollback leg (memory and disk both stay at server truth); flavor-B commit-leg pin (the delete mask keeps memory consistent with the committed delete — this pin IS the rejection of candidate d) + flavor-B rollback-leg pin (the BOUNDED divergence: foreign write survives to disk, clobbered in memory on rollback). **Pre-fix failure pinned: 2/5 verified failing against the unfixed source** (both flavor-A `store.has` masking assertions returned `true` — the stale `v1` paged in; the 3 pins pass by design pre and post).
- [docs] ADR-016 (`docs/adr/016-optimistic-put-mask-and-the-foreign-write-boundary.md`, amends ADR-015 rule 4); ADR-015's rule-4 boundary paragraph now points at ADR-016 as the resolution of the evicted-put hole; LESSONS.md entry (a fresh-wins guard that depends on residency is not a mask — eviction is exactly the case it can't cover). `enablePersistence` docstring updated (the mask covers PUT and DELETE).
- [review] Self-review sweep: overlapping/nested txs touching one key (`buffer.has` vetoes if ANY open tx holds the key — correct while any is unsettled); `tx.set→evict→tx.set` again (re-adds to memory + buffer, mask still vetoes on a subsequent evict); manifest keys never enter `pendingTx` so `readManifestRow` is untouched; synthesis path (rule 3) only fires for keys with a dirty/in-flight save — a tx-buffered key has neither, and hydrateRow re-derives the mask so it can't leak; dispose mid-tx (`pendingTx.clear()` then `disposed` gates all hydration); fresh-wins still last (resident optimistic puts blocked by `store.has`, mask matters only once residency is lost). No existing test asserted an evicted-optimistic-put key pages in the engine row (that would assert the bug) — the broadened mask leaves all 305 prior core tests green.
- Verified: `CI=true pnpm -r test` 339/339 (305 → 310 core, +5; 29 mcp unchanged), `pnpm -r typecheck` / `pnpm -r lint` / `pnpm -r build` all green. Scope note: engines/, sqlite-worker.ts, matcher/matcher-view, transactions.ts untouched — the entire fix is 1 predicate + 2 comment/docstring hunks in `src/persist.ts`.

## [2026-07-21] — DAN-629: the pending-truth overlay now covers the in-flight window (branch `tackle/dan-629-inflight-overlay`)

- [fix] `persist.ts` — closes the persist.ts in-flight-window family (spec-then-fix on the durability seam; the boundary both ADR-013 and ADR-014 documented as "the in-flight-overlay follow-up"). `flush()` drained `dirtySaves`/`dirtyDeletes` synchronously at entry, BEFORE `await engine.writeBatch` — so for the batch's whole flight, confirmed-but-undurable truth was invisible to `hydrateRow`'s overlay and to `readManifestRow`. Executed flavors, all in-session divergence (disk/reboot stayed correct): a `hydrateScope` racing an in-flight delete resurrected the removed entity into memory for the rest of the session (worst); an in-flight save + evict paged stale v1 back while disk converged to v2; a never-flushed entity evicted pre-flush was invisible to `hydrateScope` (engine has no row, so the overlay had nothing to overlay); the module's own `visibilitychange`/`beforeunload` listeners calling `flush()` mid-boot made a hidden tab an AUTOMATIC entry (DAN-630 gauntlet F1 — sharpest: the delete was drained AND durably acked before boot's stale load snapshot resolved); and `tx.remove()` racing boot let the stale snapshot un-delete an optimistic projection mid-transaction, leaving memory keeping a row disk deletes after commit (gauntlet F2).
- [feat] ADR-015 semantics: **pending truth ends only at durable acknowledgment.** (1) Drain is a MOVE, not a clear — the drained batch lands in `inflightPuts`/`inflightDeletes`, and the overlay precedence is: open-tx delete mask → dirty (newest confirmed) → in-flight (drained, possibly acked) → engine row. (2) Clearing is quiescence-gated (`maybeClearInflight`): no batch in flight AND boot settled AND no `hydrateScope` bracket open — this, not mere in-flight tracking, is what closes F1 (an acked delete must stay visible to a straddling stale snapshot); on engine failure the overlay is never cleared (frozen record of truth the engine lost keeps the session's hydrations honest). (3) `hydrateScope` synthesizes manifest-declared keys whose only copy is a pending save (dirty or in-flight) — the manifest declares residency, so the set→evict-before-first-flush window can no longer hide an entity. (4) An uncommitted optimistic DELETE masks hydration of its key (amends ADR-013's pendingTx note: unconfirmed state still never flushes/never hydrates, but it can VETO hydration); commit graduates normally, rollback lifts the mask. Optimistic PUTs need no mask (fresh-wins already blocks). `readManifestRow` consults the full overlay and re-checks AFTER its engine read (a manifest write landing mid-load outranks the loaded row). Alternatives weighed and rejected in the ADR: per-read overlay snapshot (misses truth confirmed mid-read-flight — exactly F1); awaiting the in-flight batch before hydration reads (drain-mid-read hole remains, adds latency); engine-level read-ordering contract (protocol change, DO-NOT-TOUCH); bounding F2 to tx-graduation semantics (the mask is projection integrity, not graduation — commit/rollback flows untouched).
- [test] `src/inflight-overlay.spec.ts` (11) — new `gateWrites` idiom (holds `writeBatch` in flight; reads pass through) + `gateBootStale` (captures load rows EAGERLY then holds delivery — the stale-snapshot interleaving `gateBoot` cannot produce): delete-in-flight no-resurrection (memory + disk + reboot); save-in-flight pages v2 not stale v1; in-flight manifest row outranks the engine's stale manifest (no retention resurrection under a dropped reference); synthesis dirty + synthesis in-flight; mid-boot remove + direct flush not resurrected by the stale boot snapshot (F1); mid-boot set+flush+evict hydrates the retained v2 (the DAN-621 staleness class reopened through the acked window); F2 commit leg (mask holds, memory and disk agree post-commit) + rollback leg (mask lifts, untouched durable row pages back in); two pins (flush concurrency contract unchanged; quiesced overlay does not shadow newer durable truth across two full flush cycles). **Pre-fix failure pinned: 9/11 verified failing against the unfixed source** (the 2 pins pass by design).
- [docs] ADR-015 (`docs/adr/015-pending-truth-ends-at-durable-ack.md`); ADR-013's boundary bullet and pendingTx note + ADR-014's direct-flush boundary bullet now point at the closure; LESSONS.md entry (drain-then-await hides truth — a drain in an async pipeline must stay observable until quiescence); stale `booting`/scheduleFlush comments updated to the ADR-015 division of labor (timer deferral = churn economy + first line; retained overlay = the guarantee).
- [review] Self-review sweep: overlapping flushes (`flushing` serializes batches; the await-then-reflush recursion merges consecutive drains into the in-flight sets, latest-op-per-key); hydrate racing flush's `finally` (the `activeHydrations` bracket spans read AND consume, so the finally's clear defers — decrement-then-clear happens only after the hydrate loop consumed its rows); dispose mid-flight (dispose's final flush drains pre-`disposed=true`, clears at its own finally; an open hydrateScope bracket defers that clear and its own post-await `disposed` check returns 0); gc sweep (flush-first unchanged; sweep evicts only after ack, and a subsequent hydrateScope reads post-batch engine state or the retained overlay — same values); memory growth (in-flight sets hold references to the already-encoded values the dirty sets held — no copies; transiently bounded by keys flushed since last quiescent instant, freed at first idle; degraded mode retains at most the final batch); encode/decode symmetry (in-flight values are the same encoded objects, `hydrateRow` decodes uniformly; manifest rows stay raw on both paths, never reach `hydrateRow`). Bounded in the ADR, not fixed: "all"-mode boot does not synthesize (mid-boot set→manual-evict with no manifest stays cold until its flush lands — no scope declared residency, gc timers defer during boot); boot's manifest-INDEX/scope-row loads stay engine-direct (a manifest write racing boot via direct flush can stale the boot scope SET; every entity row is still individually reconciled; converges via preload/hydrateScope).

## [2026-07-21] — DAN-630: boot hydration silently dropped app writes racing the engine loads (branch `tackle/dan-630-boot-hydration-write-drop`)

- [fix] `persist.ts` no longer drops app writes that land while boot hydration's engine loads are in flight (spec-then-fix on the durability seam; the hole DAN-621's self-review flagged as "found, NOT fixed"). Boot held `isHydrating = true` across every `engine.loadMany`/`engine.loadAll` await and the subscriber early-returned on ANY event under the flag — so a legitimate `set`/`remove` landing in that window (real width: IDB/SQLite load latency; apps that write on startup hit it routinely) emitted its event, was applied to memory, and never entered the dirty sets: silently non-durable, no error. The flag's SPAN was the defect, not its existence — and because the early-return sat before the transactional branch, mid-boot optimistic writes were silently dropped from the tx buffer too (a committed transaction then "graduated" a hole).
- [feat] ADR-014 semantics: **hydration exclusion by provenance, not coordinator phase.** (1) The subscriber excludes exactly the events stamped `origin: "hydration"` — the stamp hydrateRow already mints through the privileged `runWith` channel (ADR-007 §1, pinned by `origin.spec.ts`); the `isHydrating` flag is deleted outright (boot's span AND `hydrateScope`'s try/finally choreography). Provenance travels with the event, so there is no instant at which an app write is invisible to the durability pipeline — and the exclusion is robust even to a future async/batched subscribe, since origin is stamped into the event at emission. Mid-boot writes then flow into the dirty sets where the ADR-013 pending-truth overlay + fresh-wins already reconcile them against stale engine rows: `hydrateRow` skips a mid-boot-removed key (no resurrection) and `store.has` keeps a mid-boot set from being clobbered in memory. (2) The debounce and gc timers DEFER (re-arm) while boot hydration is in flight: flushing mid-boot would drain a pending delete out from under the overlay while a load snapshot predating it is still in the engine — the stale row would hydrate unopposed. Explicit `flush()` keeps its current mid-boot semantics (dispose racing boot must still flush acknowledged dirt); that residual direct-call window is the pre-existing in-flight class ADR-013 already documents, now narrowed to direct `flush()` calls (app code or the visibilitychange/beforeunload lifecycle listeners — see ADR-014's boundary bullet). Alternatives weighed and rejected in the ADR: tight-scoping the flag around the synchronous loops (behaviorally equivalent today but keeps the invariant temporal — this bug WAS the span drifting); buffer-and-replay (a second queue with its own ordering/failure semantics for a problem provenance dissolves); origin filter without the deferral (leaves the timer path open to stale-snapshot resurrection).
- [test] `src/boot-hydration-writes.spec.ts` (8) — gated-engine boot idiom (loads held unresolved, predicate-selectable so manifest tests can hold a specific boot load): all-mode mid-boot set durable after flush+reboot; all-mode mid-boot remove durable AND not resurrected into memory by the stale snapshot; mid-boot write to a stale-snapshot key wins in memory and on disk; mid-boot write auto-flushes with no explicit flush (the deferral re-arm strands nothing); manifest-mode writes during the index load (durable + clobber-proof, both fresh-wins legs asserted); manifest-mode remove during the entity-rows load (tightest window — after manifests resolved, before hydrateRow); two behavior pins (boot hydration and hydrateScope remount produce zero engine puts — no write-storm). **Pre-fix failure pinned: 6/8 verified failing against the unfixed source** (the 2 passers pin the hydration-exclusion behavior that must survive the mechanism swap).
- [docs] ADR-014 (`docs/adr/014-hydration-exclusion-by-provenance.md`); LESSONS.md entry (an exclusion window keyed to a phase flag is a span bug waiting to happen — key exclusions to provenance).
- [review] Self-review sweep: dispose() racing boot (post-await `disposed` re-checks unchanged; dispose's final flush still runs mid-boot and boot can never hydrateRow after it — the sync hydrate loops can't interleave with dispose); pre-open dirt (timer now re-arms under `booting` instead of stranding against `!opened`; the post-boot re-schedule retained as second line); re-armed timer vs post-boot re-schedule (scheduleFlush no-ops on a pending timer — no double-fire); boot failure paths (open reject → catch clears `booting` and sets `disabled`, killing the re-arm loop; load reject → `finally` clears `booting` before the catch); `removedPreBoot`/manifest verbs unaffected (they write the dirty sets directly, never rode the subscriber); INDEX_KEY (never reaches `hydrateRow` in either mode; the timer deferral also removes the automatic path into review-B1's half-seeded-index materialization — explicit-flush path pre-existing, unchanged); listener-writes-during-hydration-delivery inherit the hydration origin via the runWith stack and stay excluded, byte-for-byte the old flag behavior, now visible in the event stream.
- Verified: `CI=true pnpm -r test` 323/323 (286 → 294 core, +8; 29 mcp unchanged), `pnpm -r typecheck` / `pnpm -r lint` / `pnpm -r build` all green. Scope note: engines/, sqlite-worker.ts, matcher/matcher-view, transactions.ts untouched — the fix is 8 hunks in `src/persist.ts`, net −4 lines of mechanism.

## [2026-07-21] — DAN-621: evict cancelled the pending save — stale durable-row resurrection (branch `tackle/dan-621-persist-resurrection`)

- [fix] `persist.ts` no longer resurrects dead-lineage state after remove→set→evict within one debounce window (spec-then-fix on the durability seam; found pre-existing by the DAN-620 land gauntlet, verified at `699a951` with plain events — no `clear()` involved). The old evict branch dropped any pending save under ADR-004's "last flushed value stands" economy; that drop is sound only while the durable row holds some prior confirmed value of a SURVIVING entity — an intervening `remove` invalidates the row's lineage, and the subsequent set's save was the only thing scheduled to correct it. Cancelling it made the next boot re-hydrate the pre-remove value.
- [feat] ADR-013 semantics: **eviction has no authority over the durability pipeline.** (1) `evict` never mutates the dirty sets — a pending confirmed write always flushes; the pipeline is monotone in confirmed store truth (a dirty entry is displaced only by a newer confirmed event for its key). This kills the plain, transactional (committed remove→set net-put), and clear()-flavored (ADR-012 reentrant re-write) resurrection variants at once, with zero new state, and upgrades what re-hydrates from "last flushed value" to "last confirmed value" (plain set→evict no longer silently discards a debounce window of confirmed updates). (2) Engine reads honor the pending-truth overlay (`readManifestRow`/B4 generalized to entity rows): `hydrateRow` skips keys with a pending delete (boot can't resurrect a row the session already removed) and prefers a pending save's value over the stale engine row (evict→`hydrateScope` remount inside the dirty window pages in the confirmed value). Uncommitted `pendingTx` buffers deliberately excluded — unconfirmed state neither flushes nor hydrates. Alternatives weighed and rejected in the ADR: taint-gated cancel (new bookkeeping lifecycle incl. per-transaction removed-key tracking, all to preserve an economy that is nearly dead code — the only gc trigger pre-flushes, so the drop only ever fired on manual evicts, exactly where it was dangerous); delete-carry-through (all three engines apply puts THEN deletes — a same-key put+delete batch destroys the re-set value; fixing that means touching the engine protocol); dirty-generation counter (taint semantics, heavier machinery).
- [test] `src/evict-resurrection.spec.ts` (9) — the F3 exact repro (memory engine, remove→set→evict→flush→reboot ⇒ survivor value), remove→set→evict→re-set, clear()-flavored (reentrant re-write during the drain then evict), transactional flavor (tx remove→set commit graduates a net-put, evict must not cancel it), set→set(v2)→evict freshness (pins the ADR-013 supersession of "last flushed value stands"), two delete-carry pins (remove→evict; remove→set→remove→evict), and the two overlay legs (pre-ready remove not resurrected at boot; hydrateScope inside the dirty window hydrates the pending value and it survives reboot). **Pre-fix failure pinned: 6/9 verified failing against the unfixed source** (the 3 passers deliberately pin behavior that already held: re-set-after-evict self-repairs, and both delete-carry cases).
- [docs] ADR-013 (`docs/adr/013-evict-has-no-authority-over-durability.md`, amends ADR-004's drop clause only — the evict/remove split is untouched); stale `scheduleGcSweep` comment updated (flush-first ordering retained as a durability-before-eviction nicety, no longer load-bearing); LESSONS.md entry (a cache-layer event must never cancel a truth-layer correction).
- [review] Self-review sweep: evict branch's early-return audited against every dirt-stranding path (pre-open dirt is rescued by the post-boot re-schedule; in-flight arrivals by flush's finally — nothing relied on evict's scheduleFlush); overlay⇔boot interaction (only pre-open confirmed dirt can exist during hydration; manifest keys never reach `hydrateRow`; INDEX_KEY enters dirtySaves only at flush materialization); encode/decode symmetry of pending values verified through both hydrateScope and reboot legs. **Pre-existing hole found, NOT fixed (own ticket needed):** boot's `isHydrating = true` spans the `loadAll`/`loadMany` awaits, so an app write landing during those awaits is silently dropped from persistence (subscriber early-returns) — a separate event-loss class, orthogonal to eviction; `hydrateScope` already scopes the flag correctly around its synchronous loop.
- Verified: `CI=true pnpm -r test` 315/315 (277 → 286 core, +9; 29 mcp unchanged), `pnpm -r typecheck` / `pnpm -r lint` / `pnpm -r build` all green. Scope note: engines/, sqlite-worker.ts, matcher/matcher-view, transactions.ts untouched — the entire fix is 3 hunks in `src/persist.ts`.

## [2026-07-21] — DAN-620: clear() reentrancy — atomic snapshot semantics, no silent wipe (branch `tackle/dan-620-clear-reentrancy`)

- [fix] `store.clear()` is now reentrant-safe (spec-then-fix, the DAN-602/0.3 pattern — semantics decided and filed BEFORE the patch). The old shape (per-type id drain → trailing `typeMap.clear()` + `refCounts.clear()`) silently erased any write a listener applied during the drain — applied, evented, then wiped with NO event, diverging every event consumer from store truth (executed repro: DAN-606 land gauntlet, finding 1) — and destroyed retention bookkeeping wholesale, including drain-time retains and the memory-absent cold-row pins `persist.ts`'s manifest-coordinator doc already promised survive a `clear()`.
- [feat] ADR-012 semantics: **clear() removes exactly its snapshot; reentrant writes survive.** All `(type, id)` entries are captured atomically at entry (the old lazy per-type snapshot gave a write to a later type different fate than one to an earlier type) and removed via the ordinary per-entity evented path — no bulk wipe of any kind. Refcount cleanup is keyed deletion for ALL snapshotted ids, completed BEFORE any emission begins (land-gauntlet F1 hoist — the per-turn variant destroyed CROSS-ID retains established during earlier deliveries: order-dependent behavior for one listener pattern, caught by the reviewer's executed repro and pinned by a regression test verified failing against it), so listener-established pins — same-id and cross-id — and cold-key pins survive; a snapshotted-but-not-yet-drained id touched mid-drain is still cleared, with the reentrant value as honest `previousData`. Alternatives (refuse-loudly; evented bulk wipe; decrement-by-snapshot-count) weighed and rejected in the ADR — refuse breaks the H5 listeners-may-write contract nondeterministically; decrement double-counts against scopes that self-release. Behavior shift documented: clear() guarantees "everything present at call time was removed and evented," not "empty on return" (identical when no listener writes); phantom refs minted mid-drain now survive (subscribe-before-data no longer silently severed); nested clear() safe by construction (inner snapshot removes, outer takes the C1 tombstone path).
- [review] Land gauntlet (fresh reviewer, executed repros incl. pre-fix verification of all 10 tests in a throwaway worktree): one BLOCKING find (the F1 cross-id retain window above — fixed via the hoist + regression test), two boundary clarifications folded into ADR-012 (F4 "survives" precision; F2 phantom-ref-before-clear punctures the cold-row-pin guarantee — semantic question routed to DAN-602), one PRE-EXISTING persist.ts find (remove→set→evict within one debounce window resurrects stale durable state — verified at `699a951` with no clear() involved) → cut as its own ticket. Perf measured: ~1.3–1.5× on a 10K-entity clear (old path was already per-entity evented; logout-scale, acceptable).
- [test] `src/clear-reentrancy.spec.ts` (11) — reentrant set of a snapshotted id / a novel id (same + new type), reentrant retain (+ pin-without-re-add), cold-row pin survival, stream replayability through a reentrant clear (replay = source, the event-ordering invariant), the DAN-606 finding-1 repro verbatim (live encodable matcher view + reactive re-add: view === store truth, integrity guard reports zero divergence, view's pin intact at exactly 1, dispose releases), mid-drain phantom-ref survival, nested clear(), no-listener baseline. **Pre-fix failure pinned: 7/10 verified failing against `699a951`** (the 3 passers deliberately pin behavior that already held: honest not-yet-drained clearing, nested-clear safety, empty baseline).
- [docs] ADR-012 (`docs/adr/012-clear-reentrancy-snapshot-semantics.md`); LESSONS.md created (first entry: snapshot-then-bulk-clear is never reentrant-safe when listeners can write — any unevented cleanup after a listener-visible loop is a divergence bug by construction).
- [review] Self-review adjacent-hole sweep: persist remove→set same-key ordering verified last-writer-wins (the reentrant survivor reaches disk correctly); listener-Set mutation mid-drain safe under JS Set iteration; nested clear() test-pinned. Out of scope, noted on DAN-620: matcher-view `dispose()` mid-drain (ADR-010 surface, DO-NOT-TOUCH here), clear()-inside-an-open-transaction erasure semantics (literally DAN-602's title; `history.spec.ts` pins today's caveat).
- Verified: `CI=true pnpm -r test` 306/306 (266 → 277 core, +11 incl. the F1 regression; 29 mcp unchanged), `pnpm -r typecheck` / `pnpm -r lint` / `pnpm -r build` all green; observe-run drives the BUILT `dist/index.mjs` through finding-1 end-to-end (view seed → reactive re-add during clear → view === store truth → zero divergence → refcount exactly 1 → stream replay reproduces state → cold-row pin survives): 9/9 checks PASS. Scope note: engines/, sqlite-worker.ts, matcher/matcher-view untouched.

## [2026-07-20] — DAN-580 Stage 2c: read-only in-page MCP agent surface (`packages/mcp`, branch `tackle/dan-580-mcp-agent-surface`)

- [feat] `packages/mcp` (`colada-db-mcp`, private) — the first real second workspace member (workspace gains `packages/*`) and the ADR-008 §2 agent-surface edge package: core gains ZERO MCP awareness and needed ZERO new re-exports. `createColadaDbMcpServer({boundary, entityDefs, allowedTypes, history?})` returns an official-SDK low-level `Server`; the agent's `Client` connects over `InMemoryTransport.createLinkedPair()` — a real MCP session in the page (browser pages can't accept stdio/streamable-HTTP; the durable store is origin-private anyway; external bridging explicitly out of scope). ADR-011 filed (low-level `Server` over `McpServer` so `zod` never becomes our dependency and the tool list stays ONE readable literal).
- [feat] Deny-by-default writes, STRUCTURAL: zero write tools registered — a write attempt is an unknown tool with no handler to reach. Tests assert the exact tool list, no write-shaped names, and a bounced `write_entity` call with the store byte-identical (the launch-demo headline claim, true in code).
- [feat] Explicit per-type allowlist (`allowedTypes`, required; empty = maximally denied; ADR-011 records why explicit beats `local`-flag inference): schema resource (`colada-db://schema`, snapshot-at-creation from the dogfooded `exportSchema` JSON) omits hidden types AND scrubs visible types' relations targeting them; query/history refuse non-allowlisted types with a `TYPE_NOT_ALLOWED` refusal that is NOT an existence oracle (identical message either way, tested).
- [feat] `query_entities`: optional matcher-AST filter validated fail-closed via `parseMatcher` (ADR-009 dogfooded at the agent boundary) — malformed/unknown-operator/over-budget input refused with the `MatcherParseError` surfaced VERBATIM (code + `$`-path); `evaluateMatcher` over `boundary.getEntities` snapshots; envelopes honestly scoped `memory-projection` with honest truncation (default limit 100, max 1000). `read_history`: registered ONLY when a `HistoryStore` is provided (the tool list never lies about capability); purged rows arrive as data-free markers (erasure honored end-to-end, tested). All refusals in-band (`isError` + `{error:{code,message}}`) so the agent can self-correct.
- [feat] Untrusted-content marking, three redundant layers (MCP has no standard annotation yet): in-band envelope (`untrusted: true` + notice), `_meta["colada-db/untrusted"]` on results and on content blocks; README agent-surface section states plainly that marking labels but cannot compel a model.
- [docs] `docs/design/mcp-agent-surface.md` (spec-first: the 29-item done-defining test list committed BEFORE implementation), ADR-011, README agent-surface section + tense flip ("not yet built" → shipped), workspace-aware dev commands.
- [chore] Dependency security gate: `@modelcontextprotocol/sdk` pinned `^1.29.0` — pre-cleared on DAN-580 (Linear comment `d1234c1a`, 2026-07-20) and re-verified at install: `npm view` → version `1.29.0`, repo `github.com/modelcontextprotocol/typescript-sdk`, maintainers incl. jspahrsummers + Anthropic staff. Only new runtime dependency. Packet defects repaired (Linear comment): playground was never a workspace member (no membership pattern existed — `packages/*` added instead), and `pnpm -r` matched zero projects (root excluded by default) → repo `.npmrc` `include-workspace-root=true` so the packet's DoD command really runs both suites.
- Verified: `CI=true pnpm -r test` 295/295 (266 core + 29 mcp; core count unchanged), `pnpm -r typecheck`, `pnpm -r lint`, `pnpm -r build` all green; observe-run drives the BUILT packages end-to-end (SDK client connect → tool list zero-write → schema allowlist → matcher-filtered query → malformed-filter refusal → hidden-type refusal → structural write bounce → history with purge honesty): 8/8 checks PASS.

## [2026-07-20] — DAN-606 Stage 2d-2: live matcher views — H4 event tier + in-memory two-tier maintenance (branch `tackle/dan-606-live-matcher-views`)

- [feat] `StoreBoundary.subscribeEvents(listener)` (arch review H4): the event-carrying tier — full `EntityEvent` payloads (data/previousData/origin/transactionId) fanned from the same single store subscription, delivered BEFORE the void tiers (derived-state maintainers settle before snapshot re-readers), same per-listener error isolation. Additive; lands ahead of any ADR-008 §3 freeze ratification (the roadmap 2.2 prerequisite). Internal WeakMap boundary→store discovery (the persist.ts optimistic-handle idiom) so first-party modules reach `retain`/`release` without widening the public adapter contract.
- [feat] `src/matcher-view.ts` (ADR-010): `createMatcherView(boundary, entityType, filter)` — the WatermelonDB two-tier design on the DAN-605 classifier. Encodable filters maintain membership PURELY from event payloads (matched-before × matches-now → noop/add/remove, the Zero-IVM 80/20) — zero projection re-scans after the seed (spy-asserted); closures run opaque via microtask-coalesced re-scans (N-write burst = one scan). Membership = ids array, `===`-stable while membership is unchanged, one new array per change (steal-list #2); `remove` AND `evict` drop membership (view = memory projection, honest boundary — same as `getByType`; worker-seeded universes stay in DAN-579). Malformed non-callable filters refused LOUDLY (`MatcherViewError`, fail-visible — never match-nothing).
- [feat] Retention: a view = a retaining scope (view-local, via `store.retain`/`release` — NOT the persisted manifest namespace; ADR-010 records why). Live members survive `gc()` sweeps; released on membership exit and `dispose()`; refcount-drift regression test. Corollary documented: members become gc-tracked (exit → evictable, the DAN-578 residency ratchet).
- [feat] Dev-mode divergence guard `verifyIntegrity`: re-scan + compare + report (`onDivergence`) + SELF-HEAL, coalesced at the microtask boundary. An encodable-tier bug can never silently diverge from re-run truth (the subsystem's worst failure class, blast-radius HIGH).
- [fix] Self-review rework 1: the guard originally compared synchronously inside event delivery and false-alarmed on reentrant-write patterns (mid-drain the delta tier lawfully lags settled state while the H5 queue still holds the correction). Deferred to the microtask boundary; regression test verified failing against the synchronous variant.
- [docs] `docs/design/live-matcher-views.md` (spec-first: the 22-item done-defining test list written and committed BEFORE implementation, the DAN-578/0.3 pattern); ADR-010 (ids-not-rows, view-local retention, evict honesty, deferred guard); README design bullet.
- [test] `matcher-view.spec.ts` (23) + 3 `boundary.spec.ts` event-tier tests — classification/seed, zero-rerun contract, opaque coalescing + throwing-predicate isolation, `===` stability incl. the optimistic-rollback leg (Dexie #2034/#2058 class) and transient-flip convergence, retention-under-gc + dispose + no-drift, notification isolation, divergence detect/self-heal + silence, drain-queue races (mid-burst view creation; reentrant-writer false-alarm pin).
- [review] Land gauntlet (fresh reviewer, executed repros): verdict LAND, 0 blocking on the branch — event-order races (reentrant writes mid-delivery, rollback interleavings, evict→rehydrate, writes around pending coalesced scans), tier-divergence sweep, retention churn ×2 views, and the 70cfb41 deferred-guard fix (judged COMPLETE, not quieter) all held. Fixed in-branch from findings: `subscribeEvents` payload isolation (per-emission shallow copy — a mutating listener poisoned later listeners; must-land before the ADR-008 §3 freeze) · minted member arrays frozen (untyped consumers could corrupt the snapshot) · JSDoc contract: `data`/`previousData` are LIVE store refs (read-only!), mid-delivery subscribe semantics, dispose-views-before-boundary ordering. Pre-existing store-side exposure found (reentrant write during `clear()` drain → silent state wipe, no event) → routed to its own spec-then-fix ticket, store.ts untouched here.
- Verified: 264/264 tests (238 → 264; 266 after land-gauntlet regression tests), typecheck, lint, build; observe-run of the BUILT `dist/index.mjs` driving one live view end-to-end (create → flips in/out → `===` on unrelated writes → retention survives gc → dispose releases): 11/11 checks PASS. Scope note: no worker/SQL tier, no cross-tab, no store/engine changes — DAN-579's remaining items.

## [2026-07-20] — DAN-605 Stage 2d-1: serializable matcher AST + fail-closed classifier (branch `tackle/dan-605-matcher-ast`)

- [feat] `src/matcher.ts` (ADR-009): JSON-pure filter AST — `eq/neq`, `gt/gte/lt/lte` (string|number bounds), `in/nin`, `exists`, `and/or/not` — with builder `M`, strict coercion-free evaluator (`evaluateMatcher`, own-properties-only, SQL-NULL-style absence: absent fields fail EVERY comparison, only `exists` sees absence, `null` is present), canonical sorted-key `serializeMatcher` (round-trip law tested), and the two-tier gate `classifyFilter` (WatermelonDB `canEncodeMatcher` pattern, steal-list #1) — `encodable` ONLY for input that fully validates via `parseMatcher`; the verdict carries the canonical frozen tree so consumers never re-trust raw input.
- [feat] Untrusted-input hardening (ASTs may arrive from agent surfaces/persisted state): parse executes no foreign code (accessor properties rejected UNREAD), rejects `__proto__`/`constructor`/`prototype` fields, depth cap 32, in-list cap 10K, unexpected keys refused, output = fresh frozen copies (no TOCTOU gap). `MatcherParseError` with code + `$.nodes[i]`-style path, fail-visible.
- [docs] `docs/design/matcher-semantics.md` — the NORMATIVE operator/absence/type-discipline table + the SQL-parity contract for the DAN-579 worker tier (compile with `IS NULL` guards + type guards + BINARY collation; when in doubt route opaque). ADR-009 filed (numbering note: roadmap's informal "ADR-009 = worker read tier" will take the next free number).
- [test] `matcher.spec.ts` — 28 tests incl. the adversarial classifier catalog (24 inputs that must all be opaque: closures, unknown/SQL-ish operators, NaN/Infinity/Date/BigInt/object values, depth bomb ×100K without stack overflow, accessor trap with executed=false assertion, prototype-pollution via JSON.parse, extra-key smuggling) + strict-semantics matrix (null-vs-absent, no numeric-string coercion, same-type-only ordering, NaN rows, empty-list identities, prototype-chain invisibility, forged-node loud throw).
- [review] Land-stage SECURITY gauntlet (escalated: parses untrusted input; fresh reviewer): verdict LAND, 0 blocking — parse bypasses (String objects, Symbol tricks, sparse arrays, length spoofing, trap bombs, revoked proxies), query-key collision (5000-tree fuzz: serialization injective), deep-freeze, and export surface ALL held with live repros. One ADVISORY fixed in-branch: aggregate cost budget `MATCHER_MAX_COST` 65,536 units (nodes + list elements) — depth/per-list caps alone didn't bound TOTAL work (4095-wide and × 10K-lists validated at ~800ms/event = install-once pay-per-write DoS); over-budget → opaque, zero correctness loss.
- [review] Adversarial pre-handoff review: all behavioral claims HELD (fail-closed under lying proxies, TOCTOU-freedom, depth cap, semantics-table coherence) — 0 blocking behavioral findings. Fixed from findings: "never executes foreign code" claim scoped honestly (Proxy traps run during any in-realm inspection — Vue `reactive()` objects ARE proxies; guarantee = CONTAINMENT: no false-encodable possible, trap-throws degrade to opaque — deliberate contract, 3 new proxy tests) · index-planted array getters rejected unread · `-0` normalized at parse (round-trip law now exact) · non-enumerable smuggled keys refused (`getOwnPropertyNames`).
- Unblocks: live queries (roadmap 2.2) two-tier observation; agent-boundary filters (Stage 2c MCP tools can accept matcher JSON, deny-by-default validation included). Scope note: no live-query wiring, no worker changes, no ADR-003 amendment — those are DAN-579's remaining items.

## [2026-07-19] — DAN-578 Stage 2b: query-driven hydration (branch `et3rnald/dan-578-…`, PR-ready)

- [feat] `StorageEngine.loadMany(keys)` — selective load on all three engines + sqlite worker protocol op; missing keys omitted, empty input = no I/O, sqlite chunks IN-binds at 500 (`engines.spec.ts` contract + 1200-key chunking test). Pre-1.0 breaking addition (ADR-004 precedent).
- [feat] Persisted scope manifests under the reserved `__cdb_manifest__:*` namespace in the existing KV table (zero engine schema changes): scope rows + an index row, ridden through the normal dirty-set flush. The index row is materialized LAZILY at flush time from a live mirror seeded from the persisted index at boot in BOTH modes — pre-boot writes and "all"-mode sessions merge instead of clobbering (review B1).
- [feat] `enablePersistence({hydration: "manifest"})` — selective boot: index → scope rows → referenced entities via `loadMany` (never `loadAll`); each key retained under every referencing scope (the ADR-004 residency-ratchet fix: hydrated ≠ immortal, unreferenced = evictable). Default `"all"` mode unchanged; manifest rows filtered from hydration.
- [feat] Handle verbs: `setManifest` (persists + reconciles shrunk scopes, review A2) · `removeManifest` (releases retention + THE debounced `gc()` sweep — which flushes FIRST so evict can't destroy an unflushed write, review B2; never sweeps in degraded mode, review A3) · `hydrateScope` (post-evict re-hydration via loadMany, liveness-checked after every await, reviews B3/B4) · `preload` (pre-mount warm). `dispose()` releases all coordinator retentions.
- [feat] `playground/` — vanilla vite app on real OPFS (`pnpm play`): seeds 10,000 entities + 20 scope manifests; observed in Chromium: manifest boot hydrates EXACTLY the 1,000 referenced (42ms vs 68ms full boot), gc-after-boot evicts 0, evict→`hydrateScope` and `preload` both page 50/50 back. ALL 7 in-page checks PASS (re-observed after review rework). vite added as devDep (verified official vitejs/vite).
- [docs] Design doc `docs/design/query-driven-hydration.md` (locked before code); README design bullet; documented boundaries: type enumeration = projection not DB · `===` ends at evict · first-paint-without-preload · `clear()` = projection reset not disk wipe (behavior decision → DAN-602) · shrink releases pins without sweeping.
- [review] Pre-handoff fresh-eyes review found 4 blocking (index clobbering, sweep-before-flush data loss, post-await retention leaks, remount re-pin race) + 3 advisory — all repro-verified, all fixed in-branch except A1 (clear-vs-coordinator semantics → DAN-602), re-verified 9/9 by the reviewer's independent probes. 6 regression tests added.
- Verified: 209/209 tests, typecheck, build, lint, real-OPFS observe-run — per commit. Packet re-targeted for the decoupled repo (plugin half → chip-3 ticket; repair comment on DAN-578).

## [2026-07-19] — DAN-577 Stage 2a: the four ADR-007 trust primitives (branch `et3rnald/dan-577-…`, PR-ready)

- [feat] Origin vocabulary complete: `WriteOrigin` type (`local-mutation | query-response | sync-pull | hydration | rollback-replay | undo | agent`, open for `agent:*` identifiers) with the attribution-not-authentication caveat documented on the type. `hydration` stamped by persistence boot; `query-response` stamped by `writeEntitiesToStore`; ordinary write API still stamps nothing (`origin.spec.ts`).
- [feat] Policy gate (audit blocker #4 placement): `createOptimisticUpdates(store).useGate(gate)` — synchronous `willApply` veto runs BEFORE `tx.set`/`tx.remove` touches the live store (no snapshot, no replay-log entry, no event, no disk); `willCommit` at commit time is last-chance over the full change set (with pre-transaction `previous` values) and its veto invokes the real rollback machinery — settles as rollback, disk untouched per 0.3. `PolicyVetoError` fail-visible on both paths; first veto wins; gates removable (`policy-gate.spec.ts`).
- [feat] History store: `enableHistory(store, {maxEntries, maxBytes})` — field-level `{key, field, old→new, mutationId, origin, transactionId}` rows; remove/`clear()` PURGE retained rows (erasure/logout) leaving a data-free marker; `evict` excluded; dual bounds oldest-first, budget-honest on oversize rows; `createWriteIdGenerator` is the single seam Stage-3 HLC ids replace (`history.spec.ts`).
- [feat] Schema export: `EntityDefinition` gains declared `description/fields/relations/local`; `exportSchema(entityDefs)` emits JSON-pure `{version, defaultIdField, entities}` with `getId`/`merge` as capability flags (`schema.spec.ts`). This export is the Stage-2c MCP resource input.
- [chore] README + BATTLE-TESTED tense flip: ADR-007 primitives now truthfully SHIPPED (the AI-first audit's honesty bar); TinyBase netting explicitly not-adopted.
- Verified: 186/186 tests, typecheck, build, lint — each commit. Four commits, one per scope item.

## [2026-07-19] — 0.3: optimistic writes never touch disk until commit (Phase 0 complete)

- [fix] C3 + the fourth bug: `enablePersistence` is now transaction-aware — set/remove events carrying a `transactionId` buffer per-transaction (`pendingTx`), never entering the dirty sets; commit graduates the buffer's net effect into the normal write path, rollback discards it. Cold-entity corruption (partial optimistic data flushed over durable truth mid-debounce) and rollback's durable deletion of never-owned rows are both dead.
- [fix] The rollback-replay identity trap (resolution (a) from `docs/design/optimistic-durability.md`): `recompute` now replays each remaining active transaction under its OWN identity via `runWith({origin:"rollback-replay", transactionId})`, and step-1 server-truth restores carry the dying transaction's id — so compensating writes are discarded with its buffer and replayed optimistic state re-buffers idempotently instead of masquerading as confirmed.
- [feat] Commit signal: `OptimisticUpdates.onSettled(listener)` — settlement notifications (`{transactionId, outcome}`) fire after store effects; persistence discovers the store's handle lazily via the WeakMap-backed `createOptimisticUpdates(store)` on first transactional event. No new public store surface. `TransactionSettledEvent` exported.
- [test] `optimistic-durability.spec.ts` — the spec's 6 done-defining tests (rollback write-count-0, cold-entity byte-unchanged, commit flush, concurrent A/B rollback-commit and double-rollback, dispose drops uncommitted buffers, non-transactional regression). Verified live: 5/7 fail against pre-0.3 source.
- Verified: 163/163 tests, typecheck, build, lint. **Phase 0 (durability seam) is complete** — roadmap 0.1–0.6 all landed.

## [2026-07-19] — durability seam deep fixes + the write channel (arch-review response)

- [fix] C2: flush concurrency contract (`await flush()` = durable on resolve; drains in-flight then re-flushes) + dispose() final-flush before engine close, idempotent (`durability.spec.ts`, deterministic slow-engine harness).
- [fix] C1: tombstoned removes — `remove()` emits against memory-absent keys (idempotent instruction semantics); kills zombie resurrection of evicted entities, end-to-end verified across simulated boots (`tombstone.spec.ts`). `evict()` of nothing remains a true no-op.
- [feat] The write channel: `EntityEvent.origin?/transactionId?` + `store.runWith(meta, fn)` (nesting-safe). Transactions stamp `local-mutation`+txId; rollback restoration stamps `rollback-replay`. ADR-007 §1 substrate shipped early — C3's deep fix ("optimistic writes never touch disk until commit") now has its mechanism.
- [chore] Both independent audits filed in the project folder (AI-first + architecture review); publish plan rebuilt as `../ROADMAP-TO-PUBLISH.md` (phases 0–4; live queries promoted to Phase 2 per Danny).
- Verified: 149/149 tests, typecheck, build, lint — every commit.

## [2026-07-19] — Operation Spick-and-Span, night one: Track A complete + chip 2.5 + honesty pass

- [feat] Chip 2.5: `createOptimisticUpdates(store)` (src/transactions.ts) + `createCoalescer` (src/coalesce.ts) moved from the plugin's composables.ts, logic unchanged — TanStack-style clear-and-replay transactions, concurrency-correct commit folding. This is the substrate the Stage-2a `willCommit` gate wraps.
- [feat] A3 Safari armor: idbEngine open() per-attempt deadline + retry + memory-only degradation (WebKit 226547 — open() can hang with no callback). A4 quota armor: `requestDurableStorage()` + `enablePersistence({requestDurable})`.
- [test] A1 referential-stability suite (Dexie #2034 class) incl. rollback leg; A2 optimistic-op cleanup after failure storms (Dexie #2058 class). **Track A complete. 137 tests green** (113 → 137 tonight); typecheck/build/lint clean each commit.
- [chore] docs/BATTLE-TESTED.md added (adopted/refused/ahead receipts ledger); llms.txt added; README + BATTLE-TESTED tense-honesty pass per AI-first audit (unshipped ADR-007 primitives now clearly marked PLANNED — the audit's verdict: "architecture-true, code-false"; full audit in project folder).

## [2026-07-19] — subscription boundary (ADR-008 §3)

- [feat] `createStoreBoundary(store)` in `src/boundary.ts` — the adapter-facing contract: global / per-type / per-entity listeners fanned out from one store subscription, monotonic `getVersion()` as the external-store snapshot (no tick on no-op writes), `has()`-guarded snapshot reads that never mint phantom refs, per-listener error isolation, `dispose()`. 9 tests incl. the `useSyncExternalStore`-shaped vanilla-consumer contract test. 113 total green; typecheck/build/lint clean.
- [chore] DAN-577 re-targeted to this repo via Linear comment; discovered prerequisite chip 2.5 (optimistic-transaction system must move from plugin `composables.ts` into core before the pre-apply gate can wrap it).

## [2026-07-19] — ADR-008 design verdict + chip-2 normalization engine

- [feat] Chip 2: `normalize`/`denormalize` + helpers (`identifyEntity`, `splitEntityKey`, `writeEntitiesToStore`, walkers) extracted verbatim from the plugin's `plugin.ts` into `src/normalize.ts`; `normalize.spec.ts` came along. 104 tests green; typecheck/build/lint clean.
- [feat] ADR-008 accepted (council 2026-07-19): boring core, radical edges — port map (storage/sync/framework/agent/encryption/model/vector), `@vue/reactivity` internal behind an adapter-facing subscription boundary (TC39-swappable), Vite-playbook sequencing (Vue → vanilla → React), `encryptedEngine` planned as a FREE StorageEngine wrapper, agent demo = the acquisition wedge.
- Note: chip 3 (plugin swaps to `colada-db` dependency) is gated on the package being installable from the plugin repo — i.e., Danny's npm publish (or a git-URL dependency); a `file:` path out of the repo would break plugin CI.

## [2026-07-19] — repo born: chip-1 extraction from pinia-colada-plugin-normalizer

- [feat] Standalone engine extracted from `pinia-colada-plugin-normalizer` 0.3.0 (decision record: sweeos `core/projects/pinia-colada-normalizer-project/COMPETITIVE-PHASE-2026-07.md`). Moved verbatim where possible: `store.ts`, `persist.ts`, `pagination.ts`, `engines/` (memory / IndexedDB / OPFS SQLite + worker protocol), `sqlite-worker.ts`, and the framework-free spec files (`store.spec`, `persist.spec`, `engines.spec` — 80 tests).
- [feat] Core now imports `@vue/reactivity` (declared as a required peer) instead of `vue` — no Vue runtime dependency; adapters share one reactivity instance.
- [chore] `types.ts` reduced to the engine subset: removed `NormalizerQueryOptions`, `NormalizerPluginOptions`, `NormMeta`, `NORM_META_KEY`, and the `@pinia/colada` module augmentation (all stay plugin-side). `NormalizationResult` retained for the chip-2 `normalize`/`denormalize` move.
- [chore] Founding ADRs 003–007 adopted from the plugin repo (append-only; numbering continues here for engine decisions).
- [chore] Two spec sites replaced Vue's `nextTick` with a plain microtask flush (`Promise.resolve()`) — the tests only ever needed a tick, not the Vue scheduler.
- Verified: `CI=true pnpm test` 80/80 green · `pnpm typecheck` clean · `pnpm build` clean (tsdown, esm + dts) · `pnpm lint` clean. OPFS observe-run in Chromium deferred to chip 3 (no engine-code changes beyond import paths; the plugin's live verification from 0.3.0 still covers the identical engine files).
