# colada-db — public API report

> Generated by `scripts/check-api-report.mjs`. Do not hand-edit.
>
> This is the ADR-022 line-2 snapshot: the surface consumers were promised.
> A diff here is a compatibility event. Regenerate with `--update` in the
> same commit as the change that caused it, and say why.

## `dist/index.d.mts`

```ts
interface ColadaRef<T> {
  readonly value: T;
}
type EntityRecord = Record<string, unknown>;
type EntityKey = `${string}:${string}`;
interface EntityRegistry {}
type ResolveEntity<K extends string> = K extends keyof EntityRegistry ? EntityRegistry[K] : EntityRecord;
type WriteOrigin = "local-mutation" | "query-response" | "sync-pull" | "hydration" | "rollback-replay" | "undo" | "agent" | (string & {});
interface EntityEvent<T extends EntityRecord = EntityRecord> {
  type: "set" | "remove" | "evict";
  entityType: string;
  id: string;
  key: EntityKey;
  data: T | undefined;
  previousData: T | undefined;
  version?: string | number;
  origin?: WriteOrigin;
  transactionId?: string;
}
interface WriteMeta {
  origin?: WriteOrigin;
  transactionId?: string;
}
interface EntityStore {
  set<K extends string & keyof EntityRegistry>(entityType: K, id: string, data: EntityRegistry[K]): void;
  set(entityType: string, id: string, data: EntityRecord): void;
  replace<K extends string & keyof EntityRegistry>(entityType: K, id: string, data: EntityRegistry[K]): void;
  replace(entityType: string, id: string, data: EntityRecord): void;
  setMany(entities: Array<{
    entityType: string;
    id: string;
    data: EntityRecord;
  }>): void;
  remove(entityType: string, id: string): void;
  evict(entityType: string, id: string): void;
  update(entityType: string, id: string, updater: (existing: EntityRecord | undefined) => EntityRecord): void;
  get<K extends string & keyof EntityRegistry>(entityType: K, id: string): ColadaRef<EntityRegistry[K] | undefined>;
  get(entityType: string, id: string): ColadaRef<EntityRecord | undefined>;
  getByType<K extends string & keyof EntityRegistry>(entityType: K): ColadaRef<EntityRegistry[K][]>;
  getByType(entityType: string): ColadaRef<EntityRecord[]>;
  getEntriesByType(entityType: string): Array<{
    id: string;
    data: EntityRecord;
  }>;
  has(entityType: string, id: string): boolean;
  subscribe<K extends string & keyof EntityRegistry>(listener: (event: EntityEvent<EntityRegistry[K]>) => void, filter: {
    entityType: K;
  }): () => void;
  subscribe(listener: (event: EntityEvent) => void, filter?: {
    entityType?: string;
  }): () => void;
  retain(entityType: string, id: string): void;
  release(entityType: string, id: string): void;
  getRefCount(entityType: string, id: string): number | undefined;
  gc(): string[];
  runWith<T>(meta: WriteMeta, fn: () => T): T;
  clear(): void;
  toJSON(): Record<EntityKey, EntityRecord>;
  hydrate(snapshot: Record<EntityKey, EntityRecord>): void;
}
interface StorageEngine {
  isSupported(): boolean;
  open(): Promise<void>;
  loadAll(): Promise<Array<{
    key: EntityKey;
    data: unknown;
    version?: string | number;
  }>>;
  loadMany(keys: EntityKey[]): Promise<Array<{
    key: EntityKey;
    data: unknown;
    version?: string | number;
  }>>;
  writeBatch(puts: Array<{
    key: EntityKey;
    value: unknown;
  }>, deletes: EntityKey[]): Promise<void>;
  close(): void;
}
interface EntityDefinition<T extends EntityRecord = EntityRecord> {
  idField?: (string & keyof T) | (string & {});
  getId?: (entity: T) => string | null | undefined;
  merge?: (existing: T, incoming: T) => T;
  description?: string;
  fields?: Record<string, string | {
    type: string;
    description?: string;
  }>;
  relations?: Record<string, {
    entity: string;
    many?: boolean;
    description?: string;
  }>;
  local?: boolean;
}
declare function defineEntity<T extends EntityRecord = EntityRecord>(config: EntityDefinition<T>): EntityDefinition<T>;
declare const ENTITY_REF_MARKER: unique symbol;
interface EntityRef {
  [ENTITY_REF_MARKER]: true;
  entityType: string;
  id: string;
  key: EntityKey;
}
interface NormalizationResult {
  normalized: unknown;
  entities: Array<{
    entityType: string;
    id: string;
    data: EntityRecord;
  }>;
}
declare function createEntityStore(): EntityStore;
interface StoreBoundary {
  subscribe(listener: () => void): () => void;
  subscribeEntity(entityType: string, id: string, listener: () => void): () => void;
  subscribeType(entityType: string, listener: () => void): () => void;
  subscribeEvents(listener: (event: EntityEvent) => void): () => void;
  getVersion(): number;
  getEntity(entityType: string, id: string): EntityRecord | undefined;
  getEntities(entityType: string): Array<{
    id: string;
    data: EntityRecord;
  }>;
  dispose(): void;
}
declare function createStoreBoundary(store: EntityStore): StoreBoundary;
declare function splitEntityKey(key: string): [string, string];
declare function writeEntitiesToStore(entities: NormalizationResult["entities"], entityDefs: Record<string, EntityDefinition>, store: EntityStore): void;
declare function normalize(data: unknown, entityDefs: Record<string, EntityDefinition>, defaultIdField: string): NormalizationResult;
declare function denormalize(data: unknown, store: EntityStore, cache?: Map<string, {
  entity: EntityRecord;
  result: unknown;
}>): unknown;
interface PersistenceOptions {
  engine?: StorageEngine;
  dbName?: string;
  writeDebounce?: number;
  onReady?: () => void;
  onError?: (error: unknown) => void;
  requestDurable?: boolean;
  hydration?: "all" | "manifest";
}
interface PersistenceHandle {
  ready: Promise<void>;
  durable: Promise<boolean>;
  flush(): Promise<void>;
  setManifest(scopeId: string, keys: EntityKey[]): void;
  removeManifest(scopeId: string): void;
  hydrateScope(scopeId: string): Promise<number>;
  preload(scopeIds?: string[]): Promise<number>;
  dispose(): void;
}
declare function enablePersistence(store: EntityStore, options?: PersistenceOptions): PersistenceHandle;
interface IdbEngineOptions {
  dbName?: string;
  openTimeoutMs?: number;
  openAttempts?: number;
}
declare function idbEngine(options?: IdbEngineOptions): StorageEngine;
interface MemoryEngine extends StorageEngine {
  snapshot(): Map<EntityKey, {
    data: unknown;
    version: number;
  }>;
}
declare function memoryEngine(): MemoryEngine;
interface SqliteEngineOptions {
  worker: Worker | (() => Worker);
  dbName?: string;
}
interface SqliteEngine extends StorageEngine {
  readonly persistent: boolean | null;
}
declare function sqliteEngine(options: SqliteEngineOptions): SqliteEngine;
interface ProposedWrite {
  entityType: string;
  id: string;
  type: "set" | "remove";
  data?: EntityRecord;
  previous?: EntityRecord;
}
type GateVerdict = boolean | string;
interface PolicyGate {
  willApply?(change: ProposedWrite, ctx: {
    transactionId: string;
  }): GateVerdict;
  willCommit?(changeSet: ProposedWrite[], ctx: {
    transactionId: string;
  }): GateVerdict;
}
declare class PolicyVetoError extends Error {
  readonly phase: "apply" | "commit";
  readonly reason?: string;
  readonly transactionId: string;
  constructor(phase: "apply" | "commit", transactionId: string, reason?: string);
}
interface TransactionSettledEvent {
  transactionId: string;
  outcome: "commit" | "rollback";
}
interface OptimisticTransaction {
  set(entityType: string, id: string, data: EntityRecord): void;
  remove(entityType: string, id: string): void;
  commit(): void;
  rollback(): void;
}
interface OptimisticUpdates {
  apply(entityType: string, id: string, data: EntityRecord): Pick<OptimisticTransaction, "commit" | "rollback">;
  transaction(): OptimisticTransaction;
  onSettled(listener: (event: TransactionSettledEvent) => void): () => void;
  useGate(gate: PolicyGate): () => void;
}
declare function createOptimisticUpdates(store: EntityStore): OptimisticUpdates;
interface ExportedField {
  type: string;
  description?: string;
}
interface ExportedRelation {
  entity: string;
  many: boolean;
  description?: string;
}
interface ExportedEntitySchema {
  idField: string | null;
  computedId: boolean;
  customMerge: boolean;
  local: boolean;
  description?: string;
  fields: Record<string, ExportedField>;
  relations: Record<string, ExportedRelation>;
}
interface ColadaDbSchema {
  version: 1;
  defaultIdField: string;
  entities: Record<string, ExportedEntitySchema>;
}
declare function exportSchema(entityDefs: Record<string, EntityDefinition>, options?: {
  defaultIdField?: string;
}): ColadaDbSchema;
type MatcherScalar = string | number | boolean | null;
type MatcherOrderedScalar = string | number;
interface MatcherComparisonNode {
  readonly op: "eq" | "neq";
  readonly field: string;
  readonly value: MatcherScalar;
}
interface MatcherOrderedNode {
  readonly op: "gt" | "gte" | "lt" | "lte";
  readonly field: string;
  readonly value: MatcherOrderedScalar;
}
interface MatcherListNode {
  readonly op: "in" | "nin";
  readonly field: string;
  readonly values: readonly MatcherScalar[];
}
interface MatcherExistsNode {
  readonly op: "exists";
  readonly field: string;
  readonly value: boolean;
}
interface MatcherGroupNode {
  readonly op: "and" | "or";
  readonly nodes: readonly MatcherNode[];
}
interface MatcherNotNode {
  readonly op: "not";
  readonly node: MatcherNode;
}
type MatcherNode = MatcherComparisonNode | MatcherOrderedNode | MatcherListNode | MatcherExistsNode | MatcherGroupNode | MatcherNotNode;
declare const MATCHER_MAX_DEPTH = 32;
declare const MATCHER_MAX_LIST_LENGTH = 10000;
declare const MATCHER_MAX_COST = 65536;
type MatcherParseCode = "not-a-node" | "unknown-operator" | "invalid-field" | "invalid-value" | "invalid-list" | "list-too-large" | "invalid-nodes" | "unexpected-key" | "accessor-property" | "depth-exceeded" | "budget-exceeded";
declare class MatcherParseError extends Error {
  readonly code: MatcherParseCode;
  readonly path: string;
  constructor(code: MatcherParseCode, path: string, detail?: string);
}
declare function parseMatcher(input: unknown): MatcherNode;
type MatcherClassification = {
  readonly tier: "encodable";
  readonly ast: MatcherNode;
} | {
  readonly tier: "opaque";
  readonly reason: string;
};
declare function classifyFilter(input: unknown): MatcherClassification;
declare function evaluateMatcher(node: MatcherNode, entity: Record<string, unknown>): boolean;
declare function serializeMatcher(node: MatcherNode): string;
declare const M: {
  readonly eq: (field: string, value: MatcherScalar) => MatcherNode;
  readonly neq: (field: string, value: MatcherScalar) => MatcherNode;
  readonly gt: (field: string, value: MatcherOrderedScalar) => MatcherNode;
  readonly gte: (field: string, value: MatcherOrderedScalar) => MatcherNode;
  readonly lt: (field: string, value: MatcherOrderedScalar) => MatcherNode;
  readonly lte: (field: string, value: MatcherOrderedScalar) => MatcherNode;
  readonly in: (field: string, values: readonly MatcherScalar[]) => MatcherNode;
  readonly nin: (field: string, values: readonly MatcherScalar[]) => MatcherNode;
  readonly exists: (field: string, value?: boolean) => MatcherNode;
  readonly and: (...nodes: readonly MatcherNode[]) => MatcherNode;
  readonly or: (...nodes: readonly MatcherNode[]) => MatcherNode;
  readonly not: (node: MatcherNode) => MatcherNode;
};
type MatcherViewTier = "encodable" | "opaque";
type MatcherPredicate = (entity: EntityRecord) => boolean;
type MatcherViewFilter = unknown;
interface MatcherViewDivergence {
  entityType: string;
  missing: string[];
  extra: string[];
}
interface MatcherViewOptions {
  verifyIntegrity?: boolean;
  onDivergence?: (divergence: MatcherViewDivergence) => void;
}
interface MatcherView {
  readonly tier: MatcherViewTier;
  getMembers(): readonly string[];
  has(id: string): boolean;
  subscribe(listener: () => void): () => void;
  dispose(): void;
}
declare class MatcherViewError extends Error {
  readonly code = "unusable-filter";
  readonly reason?: string;
  constructor(reason?: string);
}
declare function createMatcherView(boundary: StoreBoundary, entityType: string, filter: MatcherViewFilter, options?: MatcherViewOptions): MatcherView;
interface HistoryEntry {
  key: EntityKey;
  entityType: string;
  id: string;
  field: string | null;
  old: unknown;
  new: unknown;
  mutationId: string;
  origin?: WriteOrigin;
  transactionId?: string;
  type: "set" | "remove";
}
interface HistoryOptions {
  maxEntries?: number;
  maxBytes?: number;
}
interface HistoryStore {
  list(filter?: {
    entityType?: string;
    id?: string;
    origin?: WriteOrigin;
    transactionId?: string;
  }): HistoryEntry[];
  size(): number;
  bytes(): number;
  dispose(): void;
}
declare function createWriteIdGenerator(prefix?: string): () => string;
declare function enableHistory(store: EntityStore, options?: HistoryOptions): HistoryStore;
declare function createCoalescer<T = string>(onFlush: (items: T[]) => void | Promise<void>, delay?: number): {
  add: (item: T) => void;
  flush: () => void;
};
interface RelayPageInfo {
  hasNextPage: boolean;
  hasPreviousPage: boolean;
  startCursor: string | null;
  endCursor: string | null;
}
interface CursorPaginationOptions<T extends EntityRecord = EntityRecord> {
  getCursor: (entity: T) => string | number | null | undefined;
  itemsField?: string;
  direction?: "forward" | "backward";
  dedupeKey?: string;
}
declare function cursorPagination<T extends EntityRecord = EntityRecord>(options: CursorPaginationOptions<T>): (existing: T, incoming: T) => T;
interface OffsetPaginationOptions<T extends EntityRecord = EntityRecord> {
  getOffset: (entity: T) => number;
  pageSize: number;
  itemsField?: string;
  dedupeKey?: string;
}
declare function offsetPagination<T extends EntityRecord = EntityRecord>(options: OffsetPaginationOptions<T>): (existing: T, incoming: T) => T;
interface RelayPaginationOptions {
  edgesField?: string;
  pageInfoField?: string;
  direction?: "forward" | "backward";
  dedupeByCursor?: boolean;
}
declare function relayPagination<T extends EntityRecord = EntityRecord>(options?: RelayPaginationOptions): (existing: T, incoming: T) => T;
export { type ColadaDbSchema, type ColadaRef, ENTITY_REF_MARKER, type EntityDefinition, type EntityEvent, type EntityKey, type EntityRecord, type EntityRef, type EntityRegistry, type EntityStore, type ExportedEntitySchema, type ExportedField, type ExportedRelation, type GateVerdict, type HistoryEntry, type HistoryOptions, type HistoryStore, type IdbEngineOptions, M, MATCHER_MAX_COST, MATCHER_MAX_DEPTH, MATCHER_MAX_LIST_LENGTH, type MatcherClassification, type MatcherComparisonNode, type MatcherExistsNode, type MatcherGroupNode, type MatcherListNode, type MatcherNode, type MatcherNotNode, type MatcherOrderedNode, type MatcherOrderedScalar, type MatcherParseCode, MatcherParseError, type MatcherPredicate, type MatcherScalar, type MatcherView, type MatcherViewDivergence, MatcherViewError, type MatcherViewFilter, type MatcherViewOptions, type MatcherViewTier, type MemoryEngine, type NormalizationResult, type OptimisticTransaction, type OptimisticUpdates, type PersistenceHandle, type PersistenceOptions, type PolicyGate, PolicyVetoError, type ProposedWrite, type RelayPageInfo, type ResolveEntity, type SqliteEngine, type SqliteEngineOptions, type StorageEngine, type StoreBoundary, type TransactionSettledEvent, type WriteMeta, type WriteOrigin, classifyFilter, createCoalescer, createEntityStore, createMatcherView, createOptimisticUpdates, createStoreBoundary, createWriteIdGenerator, cursorPagination, defineEntity, denormalize, enableHistory, enablePersistence, evaluateMatcher, exportSchema, idbEngine, memoryEngine, normalize, offsetPagination, parseMatcher, relayPagination, serializeMatcher, splitEntityKey, sqliteEngine, writeEntitiesToStore };
```

## `dist/sqlite-worker.d.mts`

```ts
declare function runSqliteWorker(): void;
export { runSqliteWorker };
```
